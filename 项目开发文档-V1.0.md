# 项目开发文档 V1.0

> **基于**：《项目设计文档 V1.8》（下称「设计文档」，与本文档同目录）
> **用途**：供 vibecoding 的 Agent 按阶段实施本项目，明确开发顺序、每阶段任务与可测试的验收标准。
> **元指令（沿用设计文档）**：实施过程中，若对任何原始需求有不确定、或发现逻辑矛盾、或存在两种以上技术实现路径，必须先以简洁方式提问并列出选项，等待决策。禁止猜测或默认实现。

---

## 0. 文档使用规则

### 0.1 阶段模型

- 开发流程划分为 9 个阶段：Stage 0 ~ Stage 8。每个阶段都有明确的**验收标准**，验收通过后才能进入下一阶段。
- 阶段验收 = 自动化测试/脚本检查 + 人工抽查（用户确认）。Agent 必须在每个阶段结束时展示验收结果（测试输出、接口返回、页面截图等）。
- 若验收失败，Agent 修复后重新验收；同一阶段连续两次验收失败，应停下来向用户说明原因与可选方案。
- 实施顺序：`Stage 0 → 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8`。

### 0.2 全局约定

- 目录结构以设计文档第 9 节为准；数据路径以**项目根目录**为基准（若在 `backend/` 内启动，路径相应调整，见设计文档 11.1）。
- 所有 API Key、密钥存 `.env`，确保 `.gitignore` 包含 `.env`，不提交 Git。
- 依赖锁定：`langchain==0.3.*` + `langchain-chroma` + `langchain-community`；`chromadb>=0.5`；`Flask>=3.0`；Next.js 14.x；TypeScript ≥5.0；Ant Design 5.x。
- SQLite：启用 WAL，`timeout=30`，**每个线程使用独立连接**（sqlite3 连接默认不可跨线程共享）。
- 每个阶段完成时提交一次 Git（建议分支 `codex/stage-N`），提交信息包含阶段号与验收结果摘要。

### 0.3 阶段依赖关系

| 阶段 | 名称 | 依赖 | 主要产出 |
|:--|:--|:--|:--|
| 0 | 项目骨架与基础设施 | 无 | 前后端可启动的空壳工程 |
| 1 | 数据库层 | 0 | SQLite Schema + 初始数据 + 数据访问封装 |
| 2 | 认证与用户管理 | 1 | JWT 登录/用户创建、角色鉴权、用户管理页 |
| 3 | 知识库管理（异步任务框架） | 2 | 上传/状态/列表/删除接口 + 前端知识库页 |
| 4 | 切片与灌库流水线（真实实现） | 3 | 切片器、Embedding、Chroma 入库、离线脚本 |
| 5 | 检索与权限过滤 + Reranker | 4 | 权限检索、重排、检索管线 |
| 6 | 问答与 SSE | 5 | 核心问答接口、流式输出、聊天页 |
| 7 | 测试与评测 | 6 | Golden Set、Hit Rate/MRR 评测报告 |
| 8 | 部署与答辩准备 | 7 | 线上部署、演示脚本、答辩素材 |

### 0.4 待决事项（实施到对应阶段前需用户确认）

1. **分片策略**：先按设计文档 7.1 实现；「按文件章节（Heading）分片」为待定优化，Stage 4 验收后由用户决定是否追加。
2. **评测目标**：建议 `Hit Rate ≥ 80%`、`MRR ≥ 0.5`，在 Stage 7 开始前与用户确认最终目标。
3. **Golden Set 数值**：断言数值必须与预处理后源文档一致（财务数值为虚构改写，见设计文档 10.1 注）。
4. **云服务器与 API Key**：Stage 8 前由用户提供服务器地址、Chroma 端口与生产环境密钥配置方式。

---

## Stage 0：项目骨架与基础设施

### 目标
搭建可启动、可验证的前后端工程骨架，完成环境变量与健康检查。

### 任务

- [ ] 创建 `enterprise-kb-qa/` 目录：`backend/`、`frontend/`、`docs/`、`.env`、`.gitignore`、`requirements.txt`、`README.md`。
- [ ] 后端初始化：`requirements.txt` 固定 `Flask>=3.0`、`Flask-CORS`、`langchain==0.3.*`、`langchain-chroma`、`langchain-community`、`chromadb>=0.5`、`python-dotenv`、`PyJWT`、`requests`、`openai`（DeepSeek 使用 OpenAI 兼容接口）、`gunicorn`。
- [ ] `backend/config.py`：从 `.env` 读取设计文档附录的完整环境变量清单；默认值与其一致；路径基于项目根目录解析。
- [ ] `backend/app.py`：Flask 应用工厂、CORS 配置（`FLASK_CORS_ORIGINS`）、注册后续蓝图、实现 `GET /api/health`（返回 chroma/sqlite/siliconflow_key/deepseek_key 状态，异常时 `status=degraded`）。
- [ ] 前端初始化：Next.js 14 App Router + TypeScript，安装 Ant Design 5；建立 `layout.tsx`、`login/`、`chat/`、`knowledge/`、`users/` 空页面骨架；`lib/api.ts` 基础请求封装。

### 验收标准

- [ ] `cd backend && flask run` 启动无报错；`GET /api/health` 返回 JSON，各组件字段存在（无 Key 时对应字段为未配置状态，`status=degraded` 可接受）。
- [ ] `cd frontend && npm run dev` 可访问 `/login`、`/chat` 等路由（空壳即可）。
- [ ] 目录结构符合设计文档第 9 节；`.env` 不在 Git 中。
- [ ] `requirements.txt` 可一次安装成功（`pip install -r requirements.txt`）。

### 交付物
- 可启动的 `backend/`、`frontend/`；`requirements.txt`；`.env.example`（含全部变量名，不含真实 Key）。

---

## Stage 1：数据库层

### 目标
完成 SQLite Schema、初始数据与数据访问封装，验证并发写入约束。

### 任务

- [ ] `backend/models.py`：
  - 提供每线程独立连接的工具函数（WAL、`timeout=30`）。
  - 建表：`users`、`domains`、`role_permissions`、`documents`（字段与设计文档 5.1 完全一致）。
  - 执行初始数据（设计文档 5.2）：5 个领域；权限共 16 行（employee=2、finance=3、sales=3、aftersale=3、admin=5）。
  - 封装 CRUD：用户（创建/查询/改角色/列表）、文档（插入/状态更新/查询/删除）。
- [ ] `backend/tests/test_db.py`：Schema 完整性、初始数据数量、WAL 生效、多线程独立连接并发写入不报 `database is locked`。

### 验收标准

- [ ] `python -m pytest backend/tests -q` 全绿。
- [ ] 初始化后 `app.db` 存在；5 个领域与 16 条权限记录与设计文档 2.2/5.2 一致。
- [ ] 并发写入测试通过（说明每线程独立连接约束生效）。
- [ ] 重启服务后数据仍在（持久化）。

### 交付物
- `models.py`；`test_db.py`；可重复执行的初始化/种子脚本。

---

## Stage 2：认证与用户管理

### 目标
实现管理员创建用户、登录、JWT 鉴权与角色权限控制，完成前端登录页和管理员用户管理页。

### 任务

- [ ] `backend/auth.py`：密码哈希（werkzeug）；JWT 签发/校验（`JWT_SECRET_KEY` ≥32 字符，`JWT_EXPIRATION_HOURS=168`）；`@require_auth`、`@require_admin` 装饰器。
- [ ] 认证接口（设计文档 6.1）：`POST /api/auth/login`、`GET /api/auth/me`；**不提供公开注册**。
- [ ] 用户管理接口（设计文档 6.4）：`POST /api/users`（创建用户：username/display_name/password/role，仅 admin；默认角色 `employee`）、`GET /api/users`、`PUT /api/users/:id/role`（仅 admin；role 值必须合法；admin 不能修改自己；不能将最后一个 admin 降级）、`DELETE /api/users/:id`（软删除/停用，不能删自己、不能停用最后一个可用 admin）、`PUT /api/users/:id/active`（恢复启用）、`PUT /api/users/:id/password`（重置密码 ≥6 位，旧 token 立即失效）。
- [ ] 错误响应统一为 `{"error": ..., "code": ...}`（设计文档 6.5）。
- [ ] 前端：登录页；`lib/api.ts` 封装登录/me/用户管理；token 存储与请求头 `Authorization: Bearer`；路由守卫（未登录跳 `/login`，非 admin 无法进入 `/knowledge`、`/users`）；用户管理页（列表 + 新建用户 + 修改角色 + 重置密码 + 停用/启用）。
- [ ] `backend/tests/test_auth.py`：admin 创建用户默认角色、密码过短返回 400、登录成功/密码错误、`me` 未带 token 返回 401、非 admin 访问/创建用户接口返回 403、admin 不能修改自己角色、停用后登录 403 且旧 token 失效、启用恢复、重置密码后旧密码/旧 token 失效。

### 验收标准

- [ ] 接口流程验证：admin 创建用户 → login 拿到 token → `me` 返回正确角色。
- [ ] 普通用户（employee）访问/创建用户接口返回 403；admin 访问返回 200，并可创建用户、修改角色。
- [ ] 停用账号后无法登录且旧 token 失效；启用后恢复；重置密码后旧密码与旧 token 均失效。
- [ ] 前端：未登录访问 `/chat` 跳转 `/login`；employee 登录后看不到知识库管理入口；admin 登录后可进用户管理页并改角色。
- [ ] `pytest` 相关测试全绿。

### 交付物
- `auth.py`、认证/用户管理接口、前端登录页与用户管理页、`test_auth.py`。

---

## Stage 3：知识库管理（异步任务框架）

### 目标
先实现可测试的异步灌库骨架（状态机 + 线程池 + 轮询 + 删除），真实切片/Embedding 留到 Stage 4 替换。

### 任务

- [ ] 文件上传：读取 Markdown 内容并写入 `documents.source_content`（**不再保存文件副本**）；校验扩展名为 `.md`、大小 ≤10MB、`domain` 必须存在于 `domains` 表。
- [ ] `POST /api/upload`：插入 `documents` 记录（`status=pending`，`chunk_count=0`）；用模块级 `ThreadPoolExecutor(max_workers=3)` 提交任务；立即返回 `202` + `{doc_id, status: "pending"}`。
- [ ] 后台任务（占位实现）：`pending → processing → completed`；`chunk_count` 先按文件行数统计；异常时 `failed` 并写入 `error_message`（Stage 4 替换为真实流水线）。
- [ ] `GET /api/docs`（支持 `?domain=` 过滤）、`GET /api/docs/:id/status`、`DELETE /api/docs/:id`。
- [ ] 删除接口：调用 Chroma 删除钩子（Stage 4 前可为空实现）并删除 `documents` 表记录（设计文档 5.3 同步修改要求）；允许删除 `processing` 中的文档，后台任务写入前检查记录是否存在，已删除则中止。
- [ ] 前端 `/knowledge` 页面：上传表单（文件 + 领域下拉）、每 2 秒轮询状态直至 `completed/failed`、文档列表、删除按钮（带确认）。
- [ ] 权限：上述接口全部仅 admin 可访问（除健康检查外，前端隐藏不等于后端安全）。

### 验收标准

- [ ] 上传小文件后，`status` 依次为 `pending → processing → completed`，`chunk_count` 正确。
- [ ] 并发上传 5 个文件，同一时刻后台任务数 ≤3（通过日志或测试验证）。
- [ ] 模拟任务异常（如故意抛错）时状态为 `failed` 且 `error_message` 非空。
- [ ] 删除后文档从列表消失、`documents` 表记录同步删除。
- [ ] 非 admin token 调用上传/列表/删除接口均返回 403。
- [ ] 前端轮询完成后列表自动刷新。

### 交付物
- 上传/状态/列表/删除接口；`/knowledge` 页面；异步任务框架代码。

---

## Stage 4：切片与灌库流水线（真实实现）

### 目标
实现设计文档 7.1 的 Markdown 切片器、SiliconFlow Embedding 与 Chroma 入库，替换 Stage 3 占位任务，并完成离线脚本。

### 任务

- [ ] `backend/splitter.py`：实现 `split_markdown`（设计文档 7.1）：
  - 仅识别 GFM 管道表格（每行以 `|` 开头）；文本区用 `RecursiveCharacterTextSplitter`（`chunk_size=1024`，`overlap=50`）；每个 chunk 输出 `start_line`（1-indexed，文本块用块内起始偏移换算行号）。
  - 表格整块保留；超 1024 字符时按 `header_block（表头行+分隔行）+ 数据段` 切分，**不得重复分隔行**；表头本身超限时整块存入并记录警告日志。
- [ ] `backend/tests/test_splitter.py`：纯文本、纯表格、文本+表格混合、超长表格切分、表头超长边界。
- [ ] `backend/embeddings.py`：调用 SiliconFlow `POST /v1/embeddings`（模型 `BAAI/bge-m3`，1024 维，8192 token 上限）；批量请求 + 重试；超长文本截断。
- [ ] `backend/chroma_store.py`：`PersistentClient(CHROMA_PERSIST_DIR)`，**单一 Collection**；metadata 含 `doc_id/domain/filename/chunk_id/chunk_type/start_line`；提供按 `doc_id` 删除与按 `where={"domain": {"$in": [...]}}` 检索。
- [ ] 将 Stage 3 占位任务替换为真实流水线：切片 → Embedding → 入库 → 更新 `chunk_count`；失败时 `failed + error_message`。
- [ ] `backend/ingest.py`：`python ingest.py --domain <domain> --path <path>`，复用在线流水线，增加进度条与错误重试。

### 验收标准

- [ ] `test_splitter.py` 全绿（含表格边界用例）。
- [ ] 上传或 `ingest.py` 灌入样例文档后，Chroma 中该 `doc_id` 的 chunk 数 = `documents.chunk_count`；每个 chunk 的 `metadata.domain` 与所选领域一致。
- [ ] 重启服务后向量仍在（持久化）。
- [ ] 使用错误 API Key 或断网时，任务进入 `failed` 且 `error_message` 有可读原因。
- [ ] 同一文档重复上传生成不同 `doc_id`，数据不串扰。
- [ ] 删除文档后，Chroma 中对应 `doc_id` 的向量全部清除（通过 collection count 验证）。

### 交付物
- `splitter.py`、`embeddings.py`、`chroma_store.py`、真实灌库任务、`ingest.py`、`test_splitter.py`。

---

## Stage 5：检索与权限过滤 + Reranker

### 目标
实现按角色权限过滤的检索管线与 SiliconFlow Reranker 封装。

### 任务

- [ ] `backend/rag.py`：实现 `search_with_permission`（设计文档 7.2）：
  - 从 `role_permissions` 查 `allowed_domains`；
  - `similarity_search(query, k=10, filter={"domain": {"$in": allowed_domains}})`。
- [ ] `backend/reranker.py`：`SiliconFlowReranker` 继承 `langchain.retrievers.document_compressors.base.BaseDocumentCompressor`（设计文档 3.4）：
  - `compress_documents` 调用 `POST https://api.siliconflow.cn/v1/rerank`（`model=BAAI/bge-reranker-v2-m3`、`query`、`documents`、`top_n=5`）；
  - 按返回 index 重排并保留原 metadata，按得分降序返回。
- [ ] `backend/llm.py`：DeepSeek 客户端（OpenAI 兼容，`base_url=https://api.deepseek.com`，`model=deepseek-v4-flash`）。
- [ ] `backend/tests/test_permission.py`：employee 检索结果 domain ⊆ {common, regulation}；finance ⊆ {common, finance, regulation}；sales ⊆ {common, product, regulation}；aftersale ⊆ {common, aftersale, regulation}；admin 可全领域。
- [ ] `backend/tests/test_reranker.py`：Mock API，验证降序、metadata 保留、`top_n` 生效。

### 验收标准

- [ ] `pytest` 权限与重排测试全绿。
- [ ] 对每个角色执行检索，返回文档的 `domain` 集合不超出其权限。
- [ ] Reranker 返回 Top-5，得分降序，metadata（doc_id/filename/domain）完整保留。
- [ ] 提供一个可离线演示的检索脚本：输入问题 → 输出 Top-5（标题、领域、片段）。

### 交付物
- `rag.py`、`reranker.py`、`llm.py`、`test_permission.py`、`test_reranker.py`。

---

## Stage 6：问答与 SSE

### 目标
实现核心问答接口（非流式 + 流式）与前端聊天页，这是系统的主流程。

### 任务

- [ ] 后端实现设计文档 7.3 的 `answer_question`：检索 → 重排 Top-5 → 拼接 Prompt（System + Context + History + Question）→ LLM 生成；`sources` 统一字段 `filename/domain/content_preview/doc_id/chunk_id/chunk_type/start_line`（`content_preview` 固定 200 字符）。
- [ ] Reranker 调用失败时 `/api/chat` 返回 HTTP 500（错误码 `RERANKER_ERROR`），不做降级。
- [ ] `POST /api/chat`：
  - 非流式：`{"answer": ..., "sources": [...]}`；
  - 流式（SSE）：每个 token 事件 `data: {"token": ...}`，结束事件 `data: {"done": true, "sources": [...]}`；响应头含 `Cache-Control: no-cache`、`X-Accel-Buffering: no`，`mimetype=text/event-stream`（设计文档 7.5）。
- [ ] 越权防护：后端在检索阶段按角色过滤，employee 提问财务问题不得返回财务领域内容。
- [ ] 前端 `/chat`：聊天输入、多轮历史（OpenAI 消息格式，默认传最近 10 轮 / 20 条）、`fetch + ReadableStream` 逐 token 渲染（**不使用 EventSource**）、收到 `done` 后展示引用来源卡片（SourceCard）。
- [ ] `backend/tests/test_rag.py`：Mock LLM/Reranker，验证非流式响应结构、流式事件格式、sources 截断。

### 验收标准

- [ ] 非流式调用返回合法 JSON：`answer` 非空，`sources` 每项含 `filename/domain/content_preview/doc_id/chunk_id/chunk_type/start_line`。
- [ ] 流式调用在前端逐 token 显示，最后收到 `done` 事件并展示来源卡片。
- [ ] 越权测试：employee 无法从回答或 sources 中获取 finance/product/aftersale 内容。
- [ ] 无相关资料时回答「该问题超出我的知识范围」。
- [ ] 多轮对话可引用上文（如「刚才那个问题的依据是什么」）。
- [ ] `pytest` 相关测试全绿。

### 交付物
- 问答接口（非流式 + SSE）、聊天页与来源卡片、`test_rag.py`。

---

## Stage 7：测试与评测

### 目标
补齐自动化测试，构造 Golden Set，产出可写入论文的评测报告。

### 任务

- [ ] 补齐测试：`test_auth.py`、`test_db.py`、`test_splitter.py`、`test_permission.py`、`test_rag.py` 全部通过。
- [ ] 构造 Golden Set：每个领域 15-20 个问答对，格式见设计文档 10.1；`answer_must_contain` 数值必须与预处理后源文档一致（财务数值为虚构改写，以实际文档为准）。
- [ ] `backend/eval.py`：实现 `RAGPipelineForEval`（**无权限过滤、全领域** Top-10 → Reranker → Top-5）与 `evaluate()`，输出 `hit_rate` 与 `mrr`（设计文档 10.2/10.3）。
- [ ] 生成 `docs/eval_report.md`：总体与各领域 Hit Rate/MRR 表格，并附评测日期、数据版本。
- [ ] 与用户确认最终评测目标（建议 Hit Rate ≥ 80%、MRR ≥ 0.5）；不达标时定位问题（切片质量、检索 k、重排、数据覆盖）并迭代。

### 验收标准

- [ ] `pytest` 全部通过。
- [ ] Golden Set 每领域 ≥15 条，共 5 个领域。
- [ ] `eval.py` 可一键运行并生成报告；报告含总体与分领域指标。
- [ ] 评测目标经用户确认达到或已给出明确的后续改进计划。

### 交付物
- Golden Set 数据、`eval.py`、`docs/eval_report.md`。

---

## Stage 8：部署与答辩准备

### 目标
完成本地全流程联调与线上部署，准备答辩演示素材。

### 任务

- [ ] 本地端到端验证：注册 → 登录 → 上传文档 → 灌库完成 → 问答（含 SSE）→ 删除文档。
- [ ] 云服务器部署：
  - Chroma：`docker run -p 8000:8000 chromadb/chroma`；后端切换为 `chromadb.HttpClient(host="服务器IP", port=8000)`（设计文档 11.2）。
  - 后端：Gunicorn（建议线程/异步 worker）+ Nginx 反向代理；**Nginx 关闭 `proxy_buffering`**（SSE 必需）。
  - 前端：`npm run build` 后静态导出或 Node 服务部署。
  - 数据迁移：本地 Chroma 目录与 SQLite 打包上传，注意两端 Chroma 版本一致。
- [ ] 线上验证：`/api/health` 全部 connected；完整用户旅程线上跑通（含流式问答）。
- [ ] 演示脚本：为每个角色准备 2-3 个演示问题（含越权演示，如 employee 问财务数据）；准备架构图、数据流图、权限表、评测报告截图等论文/答辩素材。

### 验收标准

- [ ] 线上 `/api/health` 返回 `status: "ok"`，四类组件均 connected。
- [ ] 线上完整旅程可复现（登录、灌库、问答、SSE、删除）。
- [ ] 演示用例清单可在 15 分钟内按脚本完整演示。
- [ ] 答辩素材齐全：架构图、核心数据流、权限模型、评测报告。

### 交付物
- 部署脚本/文档、演示脚本、答辩素材目录（`docs/`）。

---

## 9. 全局验收清单（Definition of Done）

| 阶段 | 核心验收 | 通过标志 |
|:--|:--|:--|
| 0 | 前后端可启动 + 健康检查 | `/api/health` 返回 JSON；`npm run dev` 可访问页面 |
| 1 | 数据库与种子数据 | 5 领域、16 条权限记录；并发写入测试通过 |
| 2 | 认证与权限 | 管理员创建用户/login/me、停用/启用/重置密码流程通；非 admin 访问受限接口 403 |
| 3 | 异步任务框架 | 状态机 pending→processing→completed/failed；并发 ≤3 |
| 4 | 真实灌库 | chunk_count 与 Chroma 一致；删除同步；持久化 |
| 5 | 权限检索 + 重排 | 各角色检索 domain ⊆ 权限；Top-5 降序且 metadata 完整 |
| 6 | 问答 + SSE | 非流式/流式均正确；越权不泄漏；sources 正常 |
| 7 | 测试与评测 | pytest 全绿；Golden Set 5×15+；评测报告达标 |
| 8 | 部署与答辩 | 线上健康检查 ok；完整旅程跑通；演示脚本可复现 |

---

## 10. 风险与注意事项

- **SQLite 并发**：WAL 只解决读写锁，不解决连接跨线程问题；后台任务必须各自建连接（`check_same_thread`）。
- **SSE**：`EventSource` 仅支持 GET，`/api/chat` 是 POST，前端必须用 `fetch + ReadableStream`；生产环境 Nginx 需关闭 `proxy_buffering`，Gunicorn 需线程/异步 worker。
- **Chroma**：单一 Collection + metadata 过滤；删除必须同步清理向量与 `documents` 记录；迁移时本地与服务器版本一致；客户端用 `HttpClient(host=..., port=...)`，不是完整 URL。
- **LangChain 0.3**：Chroma 集成在独立包 `langchain-chroma`；Reranker 继承 `BaseDocumentCompressor`，不要继承 `BaseRetriever`。
- **表格切片**：只支持 GFM 前导 `|` 表格；超限切分时 `header_block` 已含分隔行，不得重复；表头超限整块存入并记日志。
- **权限安全**：所有权限校验必须在后端完成；前端路由守卫只是体验优化。
- **Prompt/上下文**：history 由前端控制长度（默认不限）；sources 的 `content_preview` 固定 200 字符。
- **版本锁定**：`langchain==0.3.*` 为范围写法，安装后应把实际解析版本记录进 `requirements.txt` 的注释或锁定文件，避免环境漂移。

---

## 附录 A：验收命令速查

```bash
# 健康检查
curl http://localhost:5000/api/health

# 管理员创建用户 / 登录 / 当前用户
curl -X POST http://localhost:5000/api/users -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"username":"alice","password":"secret123","display_name":"Alice","role":"employee"}'
curl -X POST http://localhost:5000/api/auth/login -H "Content-Type: application/json" \
  -d '{"username":"alice","password":"secret123"}'
curl http://localhost:5000/api/auth/me -H "Authorization: Bearer <token>"

# 上传文档并轮询状态
curl -X POST http://localhost:5000/api/upload -H "Authorization: Bearer <admin_token>" \
  -F "file=@2024年报.md" -F "domain=finance"
curl http://localhost:5000/api/docs/<id>/status -H "Authorization: Bearer <admin_token>"

# 获取原文档全文（需登录且角色可访问该领域）
curl http://localhost:5000/api/docs/<id>/raw -H "Authorization: Bearer <token>"

# 问答（非流式）
curl -X POST http://localhost:5000/api/chat -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" -d '{"question":"2024年净利润是多少？"}'

# 问答（流式，观察 SSE 事件）
curl -N -X POST http://localhost:5000/api/chat -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" -d '{"question":"2024年净利润是多少？","stream":true}'

# 测试与评测
python -m pytest backend/tests -q
python backend/ingest.py --domain finance --path backend/markdown_src/finance
python backend/eval.py --golden docs/golden_set.json --report docs/eval_report.md
```

## 附录 B：阶段与设计文档章节对照

| 阶段 | 主要依据的设计文档章节 |
|:--|:--|
| 0 | 3 技术栈、9 目录结构、11.1、附录（环境变量） |
| 1 | 5 数据库设计（5.1/5.2/5.3） |
| 2 | 2.2 角色权限、6.1 认证、6.4 用户管理、6.5 错误格式 |
| 3 | 2.4 灌库方式、6.3 知识库管理、7.4 异步灌库 |
| 4 | 7.1 切片策略、3.3 Embedding、4.1 Chroma 架构、2.4 离线灌库 |
| 5 | 7.2 权限检索、3.4 Reranker、3.3 AI 模型 |
| 6 | 4.2 数据流、6.2 问答接口、7.3 RAG 流程、7.5 SSE |
| 7 | 10 测试与评测计划 |
| 8 | 11 部署方案、6.6 健康检查 |

---

> **文档版本**: V1.0  
> **最后更新**: 2026-08-06  
> **状态**: 待执行  
> **变更记录**: 初版，基于《项目设计文档 V1.7》编制
