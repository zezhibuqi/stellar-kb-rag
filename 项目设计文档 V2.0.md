
> **元指令**：在实施过程中，若对任何原始需求有不确定、或发现逻辑矛盾、或存在两种以上技术实现路径，必须先以简洁方式提问并列出选项，等待决策。禁止猜测或默认实现。

## 1. 项目概述

### 1.1 项目背景
毕设，面向企业真实场景的**多领域、多角色知识问答系统**。

### 1.2 项目目标
构建一个基于 RAG 的企业内部知识问答平台，支持：
- 四大业务领域（财务、规章、产品、售后）+ 公共知识，共五个知识域
- 五类用户角色，按权限隔离数据
- 文本与表格混合检索，高质量回答生成
- 订单结构化问答：对结构化订单数据用自然语言精确查询（单订单/客户/条件过滤/聚合统计）
- LLM 模型提供方可由管理员在界面切换（DeepSeek / 超算互联网 GLM-5-Base），用于生成质量对比实验
- 增强模式：由规划驱动的编排式问答（标准模式为默认链路），对需要跨文档、跨表格推理的问题拆解子问题、逐项取证后合成回答
- 会话：问答按会话组织，每用户保留最近 5 个会话，消息含模式、来源与过程记录

### 1.3 虚构公司主体
- **公司名称**：星辰科技集团（暂定）
- **公司性质**：虚构的新能源科技企业
- **说明**：所有数据来源于公开渠道并经脱敏处理，部分数据（如财务数值）为虚构改写，统一归集至该公司背景下，仅用于学术研究。

---

## 2. 功能需求

### 2.1 知识领域覆盖
| 领域标识 (domain) | 领域名称 | 典型文档         | 示例问题                |
| :------------ | :--- | :----------- | :------------------ |
| `finance`     | 财务数据 | 年报、财务报表      | "2024年净利润是多少"       |
| `regulation`  | 规章制度 | 员工手册、考勤/报销流程 | "出差报销的审批流程是什么"      |
| `product`     | 产品规格 | 产品手册、技术参数表   | "SC-100 电池的能量密度是多少" |
| `aftersale`   | 售后政策 | 售后处理手册、退换货政策 | "客户收到破损商品怎么处理"      |
| `common`      | 公共知识 | 公司通用制度、公告    | "公司的核心价值观是什么"       |

> **命名说明**：领域标识（如 `aftersale`）用于数据库键值、CLI 参数、Chroma metadata 等内部标识；领域名称（如「售后政策」）仅用于前端 UI 展示。两者不要求字面一致。

### 2.2 角色-权限模型
| 角色 (role)   | 可访问领域                             | 说明                |
| :---------- | :-------------------------------- | :---------------- |
| `employee`  | `common`,`regulation`             | 普通员工，仅访问公共知识与规章制度 |
| `finance`   | `common`,`finance`,`regulation`   | 财务人员              |
| `sales`     | `common`,`product`,`regulation`   | 销售人员              |
| `aftersale` | `common`,`aftersale`,`regulation` | 售后人员              |
| `admin`     | 全部领域                              | 系统管理员             |

### 2.3 前端页面
| 路由           | 页面     | 访问角色    | 功能描述             |
| :----------- | :----- | :------ | :--------------- |
| `/login`     | 登录页    | 所有用户    | 用户名+密码登录         |
| `/chat`      | 知识问答页  | 所有已登录用户 | 标准/增强双模式问答，获取AI回答+引用来源（含订单问答）；左侧会话列表（每用户最多 5 个） |
| `/knowledge` | 知识库管理页 | `admin` | 按领域上传/查看/删除文档    |
| `/users`     | 用户管理页  | `admin` | 查看用户列表，修改角色、停用/启用、永久删除（仅已停用账号）、重置密码 |
| `/orders`    | 订单数据页  | `aftersale`,`admin` | 浏览/筛选订单数据库列表（分页、联系方式脱敏），不提供增删改 |
| `/viewer`    | 原文档查看页 | 所有已登录用户 | Markdown 渲染原文档，从引用来源新窗口打开并高亮定位引用片段起始行 |
| `/settings`  | 模型设置页  | `admin` | 查看预设模型提供方卡片（含是否支持增强模式）、切换当前模型、测试连通性 |

> 页面级权限由前端 `LayoutWrapper` 路由守卫控制（无权限跳回 `/chat`），真实安全性由后端接口校验保证。

### 2.4 灌库方式
- **在线灌库（Web端）**：管理员通过 `/knowledge` 页面上传 Markdown 文件。后端**异步**执行切片+Embedding+入库（使用后台线程池），接口立即返回任务 ID（HTTP 202），前端通过轮询状态接口获知处理进度与结果，避免长时间等待导致 HTTP 超时。
- **离线灌库（本地脚本）**：通过命令行脚本 `python ingest.py --domain <domain> --path <path>` 批量处理，调用 SiliconFlow API 生成 Embedding（与在线灌库逻辑一致，仅增加进度条显示和错误重试机制）。

### 2.5 订单结构化问答

**订单数据字段**（SQLite `orders` 表）：

| 字段 | 说明 | 示例 |
|:---|:---|:---|
| 订单号 `order_no` | 全局唯一，格式 `DD + yyyyMMdd + 3位流水` | `DD20260315004` |
| 客户姓名 `customer_name` | 个人客户或企业名称 | `张伟` |
| 联系方式 `contact` | 11 位手机号（编造），输出一律脱敏 | `138****5678` |
| 订购物品种类 `product_type` | 产品型号，`SC-100` ~ `SC-500` 五种 | `SC-100` |
| 订购物品数量 `quantity` | 正整数 | `20` |
| 订单创建时间 `created_at` | `YYYY-MM-DD HH:MM:SS` | `2026-03-15 10:24:00` |
| 订单完成时间 `completed_at` | 可为 NULL；NULL 表示订单**未完成** | `2026-03-22 16:00:00` |
| 支付手段 `payment_method` | 枚举：`支付宝` / `微信支付` / `银行转账` / `对公转账` | `银行转账` |
| 总金额 `total_amount` | 单位：元，保留两位小数 | `17000.00` |

> 订单状态（已完成/未完成）不设独立字段，由 `completed_at` 是否为 NULL 推导。

**支持的问题类型**（面向 `aftersale` / `admin`，复用 `/chat` 页面，不新增问答入口）：

| 类型 | 示例问题 | 处理方式 |
|:---|:---|:---|
| 单订单查询 | "订单 DD20260315004 的完成情况？" | 按订单号精确查询 |
| 按客户查询 | "张伟下过哪些订单？" | 客户姓名模糊匹配 |
| 多条件过滤 | "3 月份用银行转账支付的订单有哪些？" | 时间范围 + 支付方式组合过滤 |
| 聚合统计 | "目前未完成的订单有几笔？""6 月订单总金额是多少？" | count / sum / avg 聚合 |
| 混合问题 | "订单 DD20260315004 状态如何？破损商品的退换货流程是什么？" | 订单查询 + 知识检索合并回答 |

**权限与话术**：

- `aftersale` / `admin` 可查询；`employee` / `finance` / `sales` 一律拒绝——固定回复「订单数据仅售后人员与管理员可查询，如有需要请联系售后部门。」（`sources` 为空，不调用 LLM，不执行任何查询，不泄露订单存在性）。
- 授权角色但查询无结果：固定回复「未查询到符合条件的订单。」，`sources` 附带数据库来源标记。
- 意图路由两次解析失败回退 knowledge 时，回答前缀「未能识别为订单查询，按知识库回答。」（保证可用性不劣化，同时显式暴露路由失败）。

**非目标**：不做订单增删改接口；不实现 MCP；不把订单数据灌入向量库。

### 2.6 增强模式（编排式问答）

**模式与入口**：问答页提供「标准模式 / 增强模式」切换，默认标准模式，切换选择被记住（localStorage）；同一会话内允许逐条消息混用，每条消息记录自己所用的模式。请求经 `/api/chat` 的 `mode` 字段传入（`standard` | `enhanced`）。

**执行流程**：

1. **规划**（一次 LLM 调用）：判定意图、抽取结构化过滤条件、拆解子问题；每个子问题标注数据来源（知识 / 订单），订单类子问题自带过滤条件与聚合方式。子问题写作「实体 + 属性 + 限定条件」的检索式，不丢失量化条件（年度、温度等）。
2. **子答案**（每个子问题一次 LLM 调用，并行执行）：产出该子问题的局部答案、证据覆盖状态（充分 / 部分 / 缺失）、所依据的证据，以及可供下一轮检索使用的中间实体。
3. **有界第二轮**（最多一轮）：由两部分组成——规划阶段留下的**链式待定子问题**（带 `depends_on` 与 `{id}` 占位符），用依赖子问题的 `key_entities` 填实后执行；以及覆盖为「部分 / 缺失」的子问题的**缺口补充**检索。待定项若无法展开（依赖无结果或没有中间实体），作为证据缺失进入合成，不允许静默漏项。
4. **合成**：按子问题逐项落位生成最终回答，缺项显式写「未在知识库中找到依据」；子答案仅作为线索，事实以证据单元为准，回答中出现的数值必须能在证据中原样找到。

**可用性约束**：

- 增强模式仅支持流式（`stream=true`），非流式请求返回 400（`MODE_REQUIRES_STREAM`）。
- 仅当「当前模型」在注册表中具备 `agent_capable` 能力标志时可用；否则前端禁用切换并说明原因，后端返回 400（`MODE_UNAVAILABLE`）。系统不会在背后切换模型。
- 规划判定为单跳时静默退化为单次生成（不拆解、不走「子答案 → 合成」两段式），该判定只写入 trace，界面上不做提示。

**失败语义**：

| 情况 | 处理 |
|:---|:---|
| 规划连续两次解析失败 | 退回标准模式单跳，回答前加显式前缀 |
| 单个子答案调用失败 | 不阻塞其他子问题，该子问题标注调用失败，合成时显式说明 |
| 全部子答案失败 / 合成失败 | 退化为标准模式单跳重试一次，仍失败则按现有方式发 `error` 事件、消息置为 `failed` |
| 子问题检索为空 | 标记证据覆盖缺失并进入第二轮；第二轮仍缺失则在合成结果中显式说明 |
| 全部子问题被订单工具拒绝 | 保持「不调用 LLM、直接返回固定话术」的安全约定 |

> 原则：**故障显式提示，正常的单跳判定静默**——同一条原则的两面。

**非目标**：不引入依赖图（DAG）驱动的多跳规划；不提供让 LLM 自行浏览原文的工具（无 `document_read`）；不改变角色权限模型。

### 2.7 会话与消息

- 会话由用户显式创建（问答页左侧会话列表的「新建会话」），每用户最多保留 5 个；达到上限时新建入口禁用并提示先删除一个，删除需二次确认。
- 会话标题取首条用户问题的前 20 字（去除换行），不额外调用 LLM。
- 消息记录角色、内容、所用模式与状态（`streaming` / `completed` / `failed` / `aborted`）。
- 助手消息在流开始时即落库为 `streaming`，其 id 通过首个 SSE 事件返回前端；用户点击停止时由前端显式上报，消息置为 `aborted` 并保留已收到的部分内容。
- 增强模式的分析过程（子问题、子答案、证据覆盖、耗时、模型）作为 trace 随消息保存，随会话删除。其中子问题与子答案对用户可见，明细仅供调试与评测。
- 传给 LLM 的上下文由服务端统一截取最近 10 轮（20 条），仅含最终问答文本，不含子问题卡片。

---

## 3. 技术栈

### 3.1 后端
| 组件 | 选型 | 版本要求 |
|:---|:---|:---|
| Web框架 | Flask | ≥3.0 |
| 跨域中间件 | Flask-CORS | 最新版 |
| RAG框架 | LangChain | **严格锁定 0.3.x** |
| 向量数据库 | Chroma | ≥0.5 |
| 结构化数据库 | SQLite | 标准库自带 |

### 3.2 前端
| 组件 | 选型 | 版本要求 |
|:---|:---|:---|
| 框架 | Next.js (App Router) | 14.x |
| 语言 | TypeScript | ≥5.0 |
| UI库 | Ant Design | 5.x |

### 3.3 AI 模型
| 用途        | 模型                        | 关键参数                      | 调用方式            |
| :-------- | :------------------------ | :------------------------ | --------------- |
| Embedding | `BAAI/bge-m3`             | 最大输入 8192 token，输出维度 1024 | SiliconFlow API |
| Reranker  | `BAAI/bge-reranker-v2-m3` | 最大输入 8192 token           | SiliconFlow API |
| LLM（提供方一） | deepseek-v4-flash | Context 1M；temperature=0.3（订单场景 0.0）；max_tokens=4096 | DeepSeek API |
| LLM（提供方二） | GLM-5-Base（思考型） | 同上；路由调用预留 `router_max_tokens=2000` 推理预算；不支持 `response_format` | 超算互联网 scnet（OpenAI 兼容） |

> LLM 通过**预设提供方注册表**管理，管理员可在 `/settings` 页面切换「当前模型」，回答生成与意图路由同时生效，详见 7.7 节。Embedding 与 Reranker 固定使用 SiliconFlow，不参与切换。

> **增强模式的能力要求**：注册表为每个提供方声明能力标志，增强模式要求当前模型带 `agent_capable`。该标志不是"质量排序"而是"可用性开关"——不具备该标志的提供方在增强模式下不可选中，系统不会退而求其次地换模型执行。

### 3.4 自定义组件封装（SiliconFlow Reranker）

由于 LangChain 官方未内置 SiliconFlow 的 Reranker，需自行封装。要求如下：

1. 继承 `langchain.retrievers.document_compressors.base.BaseDocumentCompressor`（**不要**继承 `BaseRetriever`，后者是主动检索器基类，与 Reranker 的"压缩/重排"职责不同）。建议类名为 `SiliconFlowReranker`。
2. 实现 `compress_documents(documents: Sequence[Document], query: str) -> Sequence[Document]` 方法。
3. 内部调用 `POST https://api.siliconflow.cn/v1/rerank`，请求体结构：
   ```json
   {
     "model": "BAAI/bge-reranker-v2-m3",
     "query": "用户问题",
     "documents": ["文档1内容", "文档2内容", ...],
     "top_n": 5
   }
   ```
4. 请求头携带 `Authorization: Bearer {SILICONFLOW_API_KEY}`。
5. 返回按得分降序排列的 Document 列表（需保留原 metadata）。

6. **失败策略**：调用失败（超时/限流/非 2xx）时抛出异常，由上层返回 HTTP 500（错误码 `RERANKER_ERROR`），不做降级。

---

## 4. 系统架构

### 4.1 逻辑分层架构图
```text
┌────────────────────────────────────────────────────────────┐
│              前端 (Next.js 14 + Ant Design)                  │
│  /login /chat /knowledge(admin) /users(admin)               │
│  /orders(aftersale,admin) /viewer /settings(admin)          │
└──────────────────────────┬─────────────────────────────────┘
                           │ HTTP (JWT Auth)
┌──────────────────────────▼─────────────────────────────────┐
│                    后端 (Flask + CORS)                       │
│  ┌──────────┐ ┌───────────────┐ ┌────────────────────┐     │
│  │ 认证鉴权  │ │ RAG 管线       │ │ 知识库管理           │     │
│  │ JWT+角色 │ │ 意图路由+检索   │ │ 上传/删除/列表       │     │
│  │          │ │ +重排+订单SQL  │ │                    │     │
│  └──────────┘ └──────┬────────┘ └────────────────────┘     │
│  ┌──────────┐ ┌──────┴────────┐ ┌────────────────────┐     │
│  │ 用户管理  │ │ 模型设置       │ │ 订单数据列表         │     │
│  └──────────┘ └───────────────┘ └────────────────────┘     │
└──────────────────────────┬─────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                                     ▼
┌──────────────────┐                 ┌──────────────────┐
│  Chroma           │                 │   SQLite          │
│ (向量存储-单集合)  │                 │ (结构化数据)       │
│                   │                 │ users             │
│ 全部domain数据    │                 │ domains           │
│ (metadata区分)    │                 │ role_permissions  │
│                   │                 │ documents         │
│                   │                 │ orders            │
│                   │                 │ app_settings      │
└──────────────────┘                 └──────────────────┘
```
> **增强模式编排器说明**：增强模式在 RAG 管线层内新增一个编排循环（`backend/orchestrator.py`），负责规划、子答案并行、有界第二轮与合成；它只通过两个工具访问数据——知识检索工具与订单查询工具，权限过滤固化在工具内部（调用方无法传入角色或领域），规划输出一律视为不可信输入。标准模式仍走原有单跳分支，两条链路共用检索、重排与权限封装。详见 7.8 节。

> **Chroma 架构说明**：采用**单一 Collection** 存储所有领域的向量数据，通过 metadata 中的 `domain` 字段区分领域。检索时使用 Chroma 的 `where` 过滤器按角色权限动态筛选，无需维护多个 Collection。

### 4.2 核心数据流：用户提问 → 回答
```
1. 前端 POST /api/chat { question, history?, stream? }
2. 后端解析 JWT → 获取 user_id + role
3. 意图路由（当前模型 LLM 调用，JSON 输出）：
   intent = order / knowledge / mixed（含结构化 filters 与 aggregation）
   解析失败重试一次，仍失败回退 knowledge 并在回答前加提示前缀
4a. order/mixed：订单权限校验（aftersale/admin）→ 参数化 SQL 查询 orders 表
    → 结果格式化为 Markdown 表格（contact 脱敏）注入上下文
4b. knowledge/mixed：查询 SQLite role_permissions → allowed_domains 列表
    → Chroma.similarity_search(question, k=10, filter={"domain": {"$in": allowed_domains}})
    → Reranker 对 Top-10 重排 → 取 Top-5
5. 拼接 Prompt (System + 订单/知识 Context + History + Question)
6. 调用当前模型 LLM → 生成回答（支持 SSE 流式输出）
7. 返回：
   - 非流式：{ answer, sources: [{filename, domain, content_preview, source_type, ...}] }
   - 流式（SSE）：逐 token 推送，最后一个事件携带 sources；生成中途失败发送 error 事件
```

**增强模式数据流**（`mode="enhanced"`，仅流式）：

```
1. 前端 POST /api/chat { question, conversation_id, mode: "enhanced", stream: true }
2. 后端解析 JWT → user_id + role；校验会话归属、mode 合法、当前模型带 agent_capable
3. 落库用户消息；创建助手消息（status=streaming），首个 SSE 事件返回 message_id 与 stage=planning
4. 规划（一次 LLM 调用）：intent + filters + aggregation + sub_questions[{id, query, source}]
   - 判定为单跳 → 短路为一次生成（只写 trace，界面不提示）
   - 解析失败重试一次，仍失败 → 退回标准模式单跳并加显式前缀
5. 子答案（每个子问题一次调用，独立线程池并行）：
     source=knowledge → knowledge_search 工具（权限过滤 + 重排 + 证据单元展开）
     source=order     → order_query 工具（白名单参数化 SQL）
     产出：子答案 + 证据覆盖 + 所依据的证据 + 中间实体；每个完成即推送 stage=sub_answer
6. 有界第二轮：仅对覆盖为「部分 / 缺失」的子问题发起，查询式由第一轮中间实体驱动
7. 合成（stage=synthesizing）：按子问题逐项落位，token 流式输出
8. 结束：done 事件携带按子问题分组的 sources，消息置 completed
   失败：error 事件，消息置 failed；用户停止则由前端上报，置 aborted
```

---

## 5. 数据库设计

### 5.1 SQLite Schema
```sql
-- SQLite 连接配置：启用 WAL 模式以支持并发写入（异步灌库场景）
-- PRAGMA journal_mode=WAL;
-- 连接超时建议设为 30 秒：connect('app.db', timeout=30)
-- 注意：每个线程/后台任务必须使用独立连接（sqlite3 连接默认不可跨线程共享）

-- 用户表
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,
    display_name TEXT,
    role TEXT NOT NULL DEFAULT 'employee',
    is_active INTEGER NOT NULL DEFAULT 1,      -- 1=启用 0=停用（删除为独立操作，见 6.4）
    token_version INTEGER NOT NULL DEFAULT 0,  -- 密码重置后 +1，旧 JWT 立即失效
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 知识领域定义
CREATE TABLE domains (
    id INTEGER PRIMARY KEY,
    name TEXT UNIQUE NOT NULL,  -- finance, regulation, product, aftersale, common
    display_name TEXT NOT NULL  -- "财务数据", "规章制度", ...
);

-- 角色-领域权限映射（核心权限表）
CREATE TABLE role_permissions (
    role TEXT NOT NULL,
    domain_name TEXT NOT NULL,
    PRIMARY KEY (role, domain_name)
);

-- 文档元信息
CREATE TABLE documents (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    filename TEXT NOT NULL,
    source_content TEXT,          -- 原文档完整 Markdown（raw 接口直接读取，不再保存文件副本）
    domain_name TEXT NOT NULL,
    chunk_count INTEGER DEFAULT 0,
    uploaded_by INTEGER REFERENCES users(id),
    uploaded_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    status TEXT DEFAULT 'pending',   -- pending | processing | completed | failed
    error_message TEXT
);

-- 订单表（结构化问答数据源，见 2.5 与 7.6）
CREATE TABLE orders (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    order_no TEXT UNIQUE NOT NULL,
    customer_name TEXT NOT NULL,
    contact TEXT,
    product_type TEXT NOT NULL,
    quantity INTEGER NOT NULL,
    created_at TIMESTAMP NOT NULL,
    completed_at TIMESTAMP,       -- NULL 表示未完成
    payment_method TEXT NOT NULL,
    total_amount REAL NOT NULL
);

-- 应用级设置（当前模型提供方等运行时可变配置）
CREATE TABLE app_settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL
);

-- 会话（每用户最多保留 5 个，见 2.7）
CREATE TABLE conversations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL REFERENCES users(id),
    title TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 会话消息（含所用模式、状态与增强模式的过程记录）
CREATE TABLE messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    conversation_id INTEGER NOT NULL REFERENCES conversations(id) ON DELETE CASCADE,
    role TEXT NOT NULL,                        -- user | assistant
    content TEXT NOT NULL DEFAULT '',
    mode TEXT NOT NULL DEFAULT 'standard',     -- standard | enhanced
    status TEXT NOT NULL DEFAULT 'completed',  -- streaming | completed | failed | aborted
    sources_json TEXT,                         -- 引用来源数组（含 sub_question_id）
    trace_json TEXT,                           -- 增强模式过程记录（子问题/子答案/覆盖/耗时/模型）
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_messages_conversation ON messages(conversation_id, id);
```

### 5.2 初始数据
```sql
-- 知识领域
INSERT INTO domains VALUES (1, 'finance', '财务数据');
INSERT INTO domains VALUES (2, 'regulation', '规章制度');
INSERT INTO domains VALUES (3, 'product', '产品规格');
INSERT INTO domains VALUES (4, 'aftersale', '售后政策');
INSERT INTO domains VALUES (5, 'common', '公共知识');

-- 权限映射（regulation 对所有角色开放，与 2.2 节保持一致）
INSERT INTO role_permissions VALUES ('employee', 'common'), ('employee', 'regulation');
INSERT INTO role_permissions VALUES ('finance', 'common'), ('finance', 'finance'), ('finance', 'regulation');
INSERT INTO role_permissions VALUES ('sales', 'common'), ('sales', 'product'), ('sales', 'regulation');
INSERT INTO role_permissions VALUES ('aftersale', 'common'), ('aftersale', 'aftersale'), ('aftersale', 'regulation');
INSERT INTO role_permissions VALUES ('admin', 'common'), ('admin', 'finance'),
       ('admin', 'regulation'), ('admin', 'product'), ('admin', 'aftersale');
```

**订单种子数据**（`backend/orders_seed.py`，`init_db()` 在表为空时写入）：

- 50 条确定性订单，时间跨度 2026-02 至 2026-07，其中 11 条未完成（`completed_at=NULL`）。
- 10 个客户：5 名个人客户（张伟、李娜、王强、赵敏、陈静）+ 5 家企业客户（如"华宇物流集团（北京）有限公司"），部分客户多次下单。
- `product_type` 覆盖 SC-100 ~ SC-500；`payment_method` 四种枚举均有分布。
- 金额 = 型号单价 × 数量，单价固定：SC-100=850、SC-200=1200、SC-300=2600、SC-400=4800、SC-500=15000（元）；完成单 `completed_at = created_at + 5 天 16:00`。
- **确定性要求**：硬编码列表，保证任何环境种子一致（评测断言依赖真实数据）；幂等，表非空时跳过。

### 5.3 Chroma Metadata
每个 Chunk 存入 Chroma 时，携带以下 metadata：
```json
{
  "doc_id": 123,              -- 对应 SQLite documents.id，用于删除时精准清理
  "domain": "finance",
  "filename": "2024年报.md",
  "chunk_id": 12,
  "chunk_type": "table",      -- "text" 或 "table"
  "start_line": 42,           -- 该 chunk 在原文档中的起始行号（1-indexed，仅定位用，不参与检索）
  "parent_type": "table",     -- "table"（整张表）或 "section"（所属章节）
  "parent_start_line": 38,    -- 证据单元在原文中的起始行号
  "parent_end_line": 61       -- 证据单元的结束行号
}
```
> **证据单元与上下文前缀**（ADR 0009）：入库文本 = 上下文前缀 + 原内容。表格块前缀为「最近标题 + 表格上方最近的非空非表格行」（向上最多回看 3 行、跳过空行、与标题去重）；文本块中同一章节内被切分出的第 2 段及以后补最近标题（第 1 段本就含标题，不重复）。前缀不计入 `chunk_size` 预算，但参与向量化与重排；`start_line` 仍指向原内容首行，定位语义不变。证据单元由 `parent_start_line` / `parent_end_line` 从 `documents.source_content` 按行切片展开。
> **证据单元去重**：按 `(doc_id, parent_start_line, parent_end_line)` 去重，避免同一张表的多个分段被各自展开、重复注入。
> **约束**：`domain` 字段的取值必须与 SQLite `domains.name` 完全一致。
> **同步修改**：`DELETE /api/docs/:id` 接口实现时，必须执行 `collection.delete(where={"doc_id": int(id)})`，并同步删除 `documents` 表中的对应记录，以确保向量、元数据与文档记录同步清理。
> **处理中删除**：允许删除 `processing` 状态的文档；后台任务每次写入向量前检查 `documents` 记录是否仍存在，若已删除则中止写入，避免孤儿向量。

---

## 6. API 接口定义

### 6.0 通用数据结构
#### 会话上下文（Chat Context）
`/api/chat` **不再接收 `history` 字段**：上下文由服务端从 `messages` 表读取并统一截取最近 10 轮（20 条），前端只提交 `question`、`conversation_id`、`mode`、`stream`。这么改的理由有二：截断口径只有一处（服务端权威），以及上下文不再由客户端提供、无法被篡改或与库内记录不一致。

标准模式下（`mode` 省略或为 `standard`）行为与 V1.9 一致，只是上下文来源由请求体改为会话记录；增强模式额外把历史上下文同时提供给规划步与合成步，且只喂最终问答文本，不喂子问题卡片。

#### 引用来源（sources）
向量来源与数据库来源统一结构，`source_type` 区分（缺省视为 `vector`，向后兼容）：
```json
{
  "source_type": "vector" ,
  "filename": "2024年报.md",
  "domain": "finance",
  "content_preview": "匹配片段前200字...",
  "doc_id": 123, "chunk_id": 12, "chunk_type": "table", "start_line": 42,
  "sub_question_id": 2
}
```
```json
{
  "source_type": "database",
  "filename": "订单数据库（SQLite）",
  "domain": "aftersale",
  "content_preview": "命中 3 条订单：DD20260315004、...",
  "doc_id": null, "chunk_id": null, "chunk_type": null, "start_line": null
}
```

### 6.1 认证接口
| 方法 | 路径 | 请求体 | 响应 | 说明 |
|:---|:---|:---|:---|:---|
| POST | `/api/auth/login` | `{username, password}` | `{token, user:{id, username, role}}` | 登录，返回 JWT（有效期 7 天） |
| GET | `/api/auth/me` | - (Header: Bearer token) | `{id, username, role}` | 获取当前用户信息 |
| PUT | `/api/auth/password` | `{old_password, new_password}` | `{token}` | 自助修改密码（所有登录用户）：验证当前密码（错误 400 `WRONG_PASSWORD`），新密码 ≥6 位；成功后 `token_version` 自增使全部旧会话失效，返回新签发 token 供当前会话无缝续用 |

> 说明：系统**不提供公开注册**；用户由管理员通过 `POST /api/users` 创建（见 6.4 节），默认角色 `employee`，密码不少于 6 位。

### 6.2 问答接口
| 方法   | 路径          | 请求体                                                  | 响应     | 说明                                    |
| :--- | :---------- | :-------------------------------------------------- | :----- | :------------------------------------ |
| POST | `/api/chat` | `{question, conversation_id, mode?, stream?}`       | 见下方说明  | 核心问答（知识+订单），自动按角色过滤，支持 SSE 流式        |

`mode` 取 `standard`（缺省）或 `enhanced`。请求必须携带 `conversation_id`（会话不存在或非本人所有返回 404）；服务端据此读取上下文并落库消息，不再接收 `history`（见 6.0）。

**请求校验**（任一不满足即返回 400，标准模式不受影响）：

| 情形 | 错误码 |
|:---|:---|
| `mode` 取值非法 | `BAD_REQUEST` |
| `mode=enhanced` 但当前模型不带 `agent_capable` | `MODE_UNAVAILABLE` |
| `mode=enhanced` 但 `stream` 不为真 | `MODE_REQUIRES_STREAM` |

**响应格式**：
- **非流式**（`stream=false` 或省略，仅标准模式）：
  ```json
  {
    "answer": "完整回答文本",
    "sources": [{"source_type": "vector", "filename": "2024年报.md", "domain": "finance", "content_preview": "匹配片段前200字...", "sub_question_id": null}]
  }
  ```
- **流式**（`stream=true`）：使用 SSE（Server-Sent Events）协议逐 token 推送，格式见 7.5 节。结束事件为 `{"done": true, "sources": [...]}`，附带 sources 信息；生成中途失败发送 `{"error": "..."}` 事件。增强模式额外推送 `stage` 过程事件；`sources` 每项带 `sub_question_id`，供前端按子问题分组（见 7.5）。

补充说明：`content_preview` 为匹配片段的截断预览（固定 200 字符），供前端展示引用来源；订单回答的 sources 附带 `source_type: "database"` 的数据库来源项（见 6.0）。

### 6.3 知识库管理接口（仅 admin）
| 方法     | 路径                     | 请求体                    | 响应                                                           | 说明                                                 |
| ------ | ---------------------- | ---------------------- | ------------------------------------------------------------ | -------------------------------------------------- |
| GET    | `/api/docs`            | -                      | `[{id, filename, domain, chunk_count, status, uploaded_at}]` | 获取所有文档列表（可加 `?domain=xxx` 过滤）                      |
| POST   | `/api/upload`          | FormData(file, domain) | `{doc_id, status: "pending"}`                               | 上传并**异步**触发灌库，立即返回 `doc_id`（HTTP 202 Accepted）；仅接受 `.md` 文件，大小 ≤ 10MB |
| GET    | `/api/docs/:id/status` | -                      | `{doc_id, status, chunk_count, error?}`                      | 查询单个文档的灌库进度（`pending/processing/completed/failed`） |
| GET    | `/api/docs/:id/raw`    | -                      | `{filename, domain, content}`                               | 获取原文档全文（需登录且角色可访问该领域；`content` 为完整 Markdown） |
| DELETE | `/api/docs/:id`        | -                      | `{message}`                                                  | 删除文档及对应向量                                          |

> 说明：`GET /api/docs/:id/raw` 不要求 admin，但必须已登录且角色可访问文档所属领域；`GET /api/docs` 列表不返回 `source_content`。

**状态轮询说明**：
- 前端在上传成功后，每 2 秒轮询一次 `/api/docs/:id/status`，直至状态变为 `completed` 或 `failed`。
- 若为 `completed`，`chunk_count` 返回实际切片数量，页面刷新列表展示新文档。
- 若为 `failed`，`error` 字段返回错误原因，前端予以提示。

### 6.4 用户管理接口（仅 admin）
| 方法 | 路径 | 请求体 | 响应 | 说明 |
|:---|:---|:---|:---|:---|
| GET | `/api/users` | - | `[{id, username, role, is_active, created_at}]` | 用户列表 |
| POST | `/api/users` | `{username, password, display_name, role}` | `{id, username, role}` | 创建用户（仅 admin；密码 ≥6 位；role 值必须合法） |
| PUT | `/api/users/:id/role` | `{role}` | `{id, role}` | 修改角色（admin 不能修改自己；不能将最后一个 admin 降级） |
| PUT | `/api/users/:id/deactivate` | - | `{id, is_active: false}` | 停用账号：不能停用自己；不能停用最后一个可用 admin |
| PUT | `/api/users/:id/activate` | - | `{id, is_active: true}` | 恢复启用停用账号（保留原密码） |
| DELETE | `/api/users/:id` | - | `{id, deleted: true}` | 永久删除：仅限已停用账号（`NOT_DEACTIVATED`）；不能删除自己；用户名随之释放，其上传文档保留（上传者置空） |
| PUT | `/api/users/:id/password` | `{new_password}` | `{id, message}` | 重置密码（≥6 位；可重置任何账号含自己；旧 token 立即失效） |

> 说明：停用与删除为两级操作（删除前置停用，防误删，见 ADR 0007）。停用后无法登录、旧 token 失效，但记录保留、用户名仍被占用；永久删除不可恢复，被删账号的 JWT 因用户不存在自动失效。删除有上传文档的用户时，文档保留且 `uploaded_by` 置空（应用层 SET NULL）。

### 6.5 订单数据接口（仅 aftersale / admin）
| 方法 | 路径 | 请求参数 | 响应 | 说明 |
|:---|:---|:---|:---|:---|
| GET | `/api/orders` | 见下表 | `{items, total, page, page_size}` | 订单列表（只读），供 `/orders` 页面使用；其余角色返回 403 |

| 参数 | 说明 |
|:---|:---|
| `order_no` / `customer_name` | 精确 / 模糊过滤 |
| `product_type` / `payment_method` / `status` | 枚举过滤（`status=completed/pending`，由 `completed_at` 推导） |
| `created_from` / `created_to` | 日期范围（含当天，`created_to` 按次日零点处理） |
| `page` / `page_size` | 分页（默认 1 / 20，`page_size` 上限 100） |

响应约定：`contact` 脱敏（前 3 位 + `****` + 后 4 位）；`status` 由 `completed_at` 推导为 `completed` / `pending`；默认按创建时间倒序。过滤逻辑与订单问答执行器（7.6）共用同一套白名单规则。

### 6.6 模型设置接口（仅 admin）
| 方法 | 路径 | 请求体 | 响应 | 说明 |
|:---|:---|:---|:---|:---|
| GET | `/api/settings/model` | - | `{active, default, providers: [...]}` | 当前模型与预设提供方列表（含密钥配置状态，不回显密钥） |
| PUT | `/api/settings/model` | `{provider_id}` | 同上 | 切换当前模型；提供方不存在返回 404，密钥未配置返回 400（`PROVIDER_KEY_MISSING`） |
| POST | `/api/settings/model/test` | `{provider_id}` | `{ok, provider_id, model, reply}` | 连通性测试（发一次最小真实调用）；失败返回 502（`PROVIDER_TEST_FAILED`） |

`providers` 数组元素：`{id, name, platform, base_url, model, api_key_configured, agent_capable, active}`。`agent_capable` 表示该提供方是否可用于增强模式（见 2.6 与 7.8），前端据此在模型卡片上显示「支持增强模式 / 仅标准模式」，并在增强模式不可用时说明原因。切换立即对后续请求生效，进行中的回答不受影响；设计细节见 7.7。

### 6.7 错误响应格式
所有错误统一返回：
```json
{
  "error": "错误描述",
  "code": "ERROR_CODE"
}
```
HTTP 状态码：400（请求错误）、401（未认证）、403（无权限）、404（不存在）、405（方法不允许）、500（服务器错误）、502（上游连通性测试失败）。

### 6.8 健康检查接口
| 方法 | 路径 | 说明 |
|:---|:---|:---|
| GET | `/api/health` | 返回系统各组件状态，用于调试和答辩演示 |

响应示例：
```json
{
  "status": "ok",
  "sqlite": {"status": "connected", "path": "..."},
  "chroma": {"status": "connected", "path": "..."},
  "siliconflow_key": {"status": "configured"},
  "llm_provider": {"id": "deepseek", "model": "deepseek-v4-flash", "status": "configured"}
}
```
任一组件异常时，`status` 变为 `"degraded"`，对应字段显示错误原因。`llm_provider` 探测**当前模型**（而非固定 DeepSeek）的密钥配置状态。

### 6.9 会话接口（已登录用户，仅限本人会话）
| 方法 | 路径 | 请求体 | 响应 | 说明 |
|:---|:---|:---|:---|:---|
| GET | `/api/conversations` | - | `[{id, title, created_at, updated_at}]` | 当前用户的会话列表，按 `updated_at` 倒序 |
| POST | `/api/conversations` | - | `{id, title}` | 新建会话；已达每用户 5 个上限时返回 400（`CONVERSATION_LIMIT`） |
| GET | `/api/conversations/:id/messages` | - | `[{id, role, content, mode, status, sources, trace?, created_at}]` | 回放会话；`trace` 仅在增强模式消息上返回 |
| DELETE | `/api/conversations/:id` | - | `{id, deleted: true}` | 删除会话及其全部消息，不可恢复（前端二次确认） |

> **归属校验**：非本人会话一律返回 404（而非 403），避免通过状态码暴露他人会话是否存在。
> **标题生成**：新建时标题为空占位，首条用户消息落库后由服务端用其前 20 字更新（去除换行），不调用 LLM。
> **删除语义**：消息随会话级联删除（`ON DELETE CASCADE`，连接已开启 `PRAGMA foreign_keys=ON`）；trace 不单独保留。

---

## 7. 核心逻辑设计

### 7.1 Markdown 分区切片策略

> **设计目标**：解决 `RecursiveCharacterTextSplitter` 切断表格导致语义丢失的问题。
>
> **格式约束**：本系统仅支持标准 GFM（GitHub Flavored Markdown）管道表格，即每行以 `|` 开头。不支持无前导 `|` 的简写表格格式。
>
> **尺寸维持不变**（ADR 0009）：`chunk_size` 保持 1024 字符。中文 1024 字符约合 1000~1400 token，已接近 bge-reranker-v2-m3 的有效判别区间；块继续放大会让向量趋近「平均脸」、重排判别力下降，而增强模式会把检索次数成倍放大、同步放大这一代价。8192 是上下文上限，不是有效区间。
> **原「按文件章节（Heading）分片」待定项就此关闭**：章节不再作为检索单元，而作为**证据单元**（小块检索、大块证据），见下方规则 6~8 与 5.3 节。

**输入/输出示例**：
```
输入 Markdown：
  ## 营收概况
  2024年公司营收稳步增长，主要得益于新能源业务扩张。
  | 产品型号 | 营收(亿元) | 同比 |
  |---------|-----------|------|
  | SC-100  | 120       | 15%  |
  | SC-200  | 200       | 22%  |

输出 Chunks：
  Chunk 1 [text] (start_line=2): "## 营收概况\n2024年公司营收稳步增长..."
  Chunk 2 [table] (start_line=5): 入库文本 = 前缀「## 营收概况」+ 表格原文
    "## 营收概况\n| 产品型号 | 营收(亿元) | 同比 |\n|---------|-----------|------|\n| SC-100  | 120       | 15%  |\n| SC-200  | 200       | 22%  |"
```

**处理规则**：
1. 逐行扫描 Markdown，识别 `|` 开头的连续行为表格区，其余为文本区。
2. 文本区：使用 `RecursiveCharacterTextSplitter`，`chunk_size=1024`，`overlap=50`。
3. 表格区：整块保留为一个 chunk。
4. 表格超限（>1024字符）时，按以下步骤切分：
   - 识别表头行（第一行）和分隔行（如 `|---|`），将它们合并为 `header_block`。
   - 将剩余的数据行按行数均分为 M 段（M = ceil(总数据行数 / 每段最大行数)，每段最大行数由 `chunk_size` 动态计算）。
   - 每段生成一个新的表格 chunk，内容为 `header_block + 该段数据行`（分隔行已包含在 `header_block` 中）。
   - **边界情况**：若表头本身（含列名）已超过 1024 字符，则不再切分数据，直接将整个表格作为一个 chunk 存入，并在日志中记录警告。
5. 每个 chunk 附带 `start_line`（1-indexed）：表格块取表头所在行；文本块通过 chunk 在块内的起始偏移换算行号。`start_line` 仅用于前端定位，不参与检索与生成。
6. **上下文前缀**（ADR 0009）：
   - 表格块：前缀 = 最近一级标题 + 表格上方最近的非空非表格行（向上最多回看 3 行、跳过空行；若该行本身就是标题则不重复拼接）。它承载章节归属与表名/单位口径，对数值类问答尤为关键。
   - 文本块：仅对「同一章节内被切分出的第 2 段及以后」补最近一级标题（第 1 段本就含标题，不重复）。只补后段而非全部，是因为给同章节所有块加相同前缀会提高它们彼此的相似度、削弱检索区分度。
   - 前缀不计入 `chunk_size` 预算，但参与向量化与重排；`start_line` 仍指向原内容首行，定位语义不变。
   - 超长表格被切成 M 段时，每一段都要带前缀，理由与重复拼接 `header_block` 相同：每段必须是自足的检索单元。
7. **证据单元**：检索命中后实际注入上下文的原文范围。表格块所属的证据单元是**整张表**；文本块所属的证据单元是**其所在章节**——按标题层级划分，从最近标题到下一个同级或更高级标题之前，文档首块之前以文档首行为起点。无标题文档退化为「命中块 + 前后各一个相邻块」。
8. **证据单元行区间**写入 metadata（`parent_start_line` / `parent_end_line` / `parent_type`）；检索命中后按该区间从 `documents.source_content` 切片展开，超过 3000 字符时以命中段为中心向两侧截取，截取落在完整行边界上，不切断表格行。证据单元按 `(doc_id, parent_start_line, parent_end_line)` 去重。

**伪代码**：
```python
def split_markdown(md_content: str) -> list[dict]:
    """仅支持 GFM 管道表格（每行以 | 开头）"""
    chunks = []
    current_type = None  # "text" | "table"
    current_lines = []
    
    for line in md_content.split('\n'):
        is_table = line.strip().startswith('|')
        new_type = "table" if is_table else "text"
        
        if new_type != current_type and current_lines:
            chunks.append({"type": current_type, "content": '\n'.join(current_lines)})
            current_lines = []
        current_type = new_type
        current_lines.append(line)
    
    if current_lines:
        chunks.append({"type": current_type, "content": '\n'.join(current_lines)})
    
    # 对表格块做超限处理
    return process_oversized_tables(chunks)
```

### 7.2 权限过滤检索逻辑

**伪代码**：
```python
def search_with_permission(query: str, user_role: str, chroma_collection=None, db_conn=None):
    """
    使用单一 Chroma Collection，通过 metadata filter 按角色权限过滤领域。
    实际实现中 chroma_collection / db_conn 由调用方注入（或来自全局配置）。
    """
    # 1. 查权限
    cursor = db_conn.execute(
        "SELECT domain_name FROM role_permissions WHERE role = ?", (user_role,)
    )
    allowed_domains = [row[0] for row in cursor.fetchall()]
    
    # 2. 检索（单 Collection + metadata 过滤）
    results = chroma_collection.similarity_search(
        query, 
        k=10, 
        filter={"domain": {"$in": allowed_domains}}
    )
    return results
```

> **说明**：`chroma_collection` 指 LangChain 的 `Chroma` 封装实例（`langchain_chroma.Chroma`），其提供 `similarity_search`；原生 chromadb Collection 的检索方法名为 `query`。

### 7.3 RAG 完整流程

> 实际实现中 `answer_question` 首先做**意图路由**（见 7.6），order/mixed 意图额外注入订单数据库上下文；以下伪代码为知识问答主路径。

**伪代码**：
```python
import json
from typing import Union, Generator

def answer_question(question: str, history: list, user_role: str, stream: bool = False) -> Union[dict, Generator]:
    # Step 0: 意图路由（见 7.6）：order/mixed 注入订单上下文，此处略
    
    # Step 1: 检索
    candidates = search_with_permission(question, user_role)
    
    # Step 2: 重排（使用自定义封装类，见 3.4 节）
    from reranker import SiliconFlowReranker
    reranker = SiliconFlowReranker()
    # compress_documents 接收文档列表和查询，返回按相关性降序排序的文档列表
    reranked_docs = reranker.compress_documents(documents=candidates, query=question)
    top5 = reranked_docs[:5]  # 取前5
    
    # Step 3: 构建 Prompt
    system_prompt = """你是星辰科技集团的内部知识助手。请根据提供的参考资料回答问题。
    如果资料中没有相关信息，请如实说"该问题超出我的知识范围"，不要编造。"""
    
    context = "\n\n---\n\n".join([doc.page_content for doc in top5])
    
    def format_history(history: list) -> str:
        """将历史消息列表拼接为上下文字符串"""
        if not history:
            return ""
        parts = []
        for msg in history:
            if msg["role"] == "user":
                parts.append(f"用户：{msg['content']}")
            elif msg["role"] == "assistant":
                parts.append(f"助手：{msg['content']}")
        return "\n".join(parts) + "\n"
    
    history_text = format_history(history)
    full_prompt = f"{system_prompt}\n\n参考资料：\n{context}\n\n{history_text}\n用户：{question}"
    
    # 构建 sources 信息（统一使用 content_preview 字段名）
    sources = [
        {
            "source_type": "vector",
            "filename": doc.metadata["filename"],
            "domain": doc.metadata["domain"],
            "content_preview": doc.page_content[:200],
            "doc_id": doc.metadata.get("doc_id"),
            "chunk_id": doc.metadata.get("chunk_id"),
            "chunk_type": doc.metadata.get("chunk_type"),
            "start_line": doc.metadata.get("start_line")
        } for doc in top5
    ]
    
    # Step 4: 生成（支持流式/非流式；上下文含订单数据时切换为订单专用 System Prompt，见 7.6）
    if stream:
        # 流式模式：逐 token 返回（SSE 事件格式见 7.5 节）
        def generate():
            for token in llm.stream(full_prompt):
                yield f"data: {json.dumps({'token': token})}\n\n"
            # 流结束，发送 sources（与 7.5 节格式一致）
            yield f"data: {json.dumps({'done': True, 'sources': sources})}\n\n"
        return generate()
    else:
        answer = llm.invoke(full_prompt)
        return {
            "answer": answer,
            "sources": sources
        }
```

### 7.4 异步灌库实现（后台任务）

由于上传文件涉及网络 API 调用（Embedding），同步处理易超时，后端采用轻量级线程池方案：

1. **任务触发**：`/api/upload` 接收到文件后，先在 SQLite `documents` 表中插入一条状态为 `pending` 的记录（`chunk_count = 0`），获取 `doc_id`。
2. **后台执行**：使用 `ThreadPoolExecutor` 提交后台任务，执行切片 → 调用 SiliconFlow Embedding API → 存入 Chroma。写入向量前先按 `doc_id` 清理旧向量（幂等，支持重灌）。
3. **状态更新**：
   - 任务开始时更新状态为 `processing`。
   - 成功完成后更新状态为 `completed`，同时写入 `chunk_count`。
   - 若发生异常，捕获后更新状态为 `failed`，并将异常信息写入 `error_message` 列。
4. **SQLite 并发写入**：SQLite 需启用 WAL 模式（`PRAGMA journal_mode=WAL`）以支持多线程并发读写，连接超时建议设为 30 秒（`sqlite3.connect('app.db', timeout=30)`）。注意：sqlite3 连接默认不能跨线程共享（`check_same_thread`），每个后台线程必须使用独立连接；任务结束后显式关闭连接（Windows 文件锁）。
5. **并发控制**：同一时刻最多允许 3 个后台任务并发（通过 `ThreadPoolExecutor(max_workers=3)` 控制），避免资源耗尽。

### 7.5 SSE 流式输出设计

为提升用户体验，问答接口支持 SSE（Server-Sent Events）流式输出，使 LLM 回答逐 token 推送至前端。

**后端实现要点**：
```python
from flask import Response, stream_with_context
import json

@app.route('/api/chat', methods=['POST'])
def chat():
    # ... 权限校验、意图路由、检索、重排等步骤同 7.3 / 7.6 ...
    
    if stream:
        def generate():
            try:
                for event in result:          # result 为 rag 层生成器，逐个产出 SSE 事件
                    yield f"data: {event}\n\n"
            except Exception:
                # 生成中途失败（如 LLM 超时/限流）：显式发送 error 事件，不静默断流
                yield f"data: {json.dumps({'error': '生成失败，请稍后重试'})}\n\n"
        
        return Response(
            stream_with_context(generate()),
            mimetype='text/event-stream',
            headers={
                'Cache-Control': 'no-cache',
                'X-Accel-Buffering': 'no'
            }
        )
    else:
        # 非流式响应（见 7.3）
        ...
```

**SSE 数据格式**：
- 每个 token 事件：`data: {"token": "部分内容"}\n\n`
- 结束事件：`data: {"done": true, "sources": [...]}\n\n`
- 失败事件（生成中途异常）：`data: {"error": "生成失败，请稍后重试"}\n\n`
- 增强模式的过程事件（`stage`，标准模式不发送）：
  - `data: {"stage": "planning", "message_id": 123}\n\n`：规划开始（首个事件，携带助手消息 id）
  - `data: {"stage": "planned", "sub_questions": [{"id": 1, "text": "..."}]}\n\n`：规划完成，给出子问题列表
  - `data: {"stage": "sub_answer", "sub_question_id": 1, "answer": "...", "coverage": "sufficient"}\n\n`：单个子问题完成即推送，并行完成顺序不固定；`coverage` 取 `sufficient` / `partial` / `missing`
  - `data: {"stage": "synthesizing"}\n\n`：进入合成阶段
  - 第二轮的补充检索不单独发事件类型，复用 `sub_answer`（携带新的子问题 id）；前端以「补充检索」标注

**前端消费方式**：
- 使用 `fetch` + `ReadableStream` 逐块读取响应（`/api/chat` 为 POST 接口，`EventSource` 仅支持 GET，不适用）。
- 逐 token 追加渲染回答内容。
- 收到 `done` 事件后展示引用来源卡片；收到 `error` 事件将该条回答标记为「生成失败，请稍后重试」。

### 7.6 意图路由与订单结构化问答

**总体流程**：

```
用户问题
   │
   ▼
┌─────────────────────────┐
│ 意图路由（当前模型，JSON 输出）│  → {"intent": "order|knowledge|mixed",
└─────────────────────────┘     "filters": {...}, "aggregation": ...}
   │                 │
   │ intent=order    │ intent=knowledge
   │ 或 mixed        │ 或 mixed
   ▼                 ▼
┌──────────────┐   ┌────────────────────────┐
│ 订单查询执行器 │   │ 现有 RAG 管线           │
│ 权限校验       │   │ 权限检索→Reranker→Top-5 │
│ 参数化SQL模板  │   └────────────────────────┘
└──────────────┘             │
   │                         │
   └────────┬────────────────┘
            ▼
   上下文合并 → Prompt → 当前模型生成（流式/非流式）
            ▼
   answer + sources（向量来源 + 数据库来源）
```

**核心模块 `backend/order_qa.py`**：

- `route_question(question, history)`：意图路由。当前模型 LLM 调用（temperature=0，JSON 输出），判定意图并抽取结构化过滤参数；输出 schema 固定 `{intent, filters, aggregation}`，附 7 条 few-shot 示例（覆盖 order / knowledge / mixed、聚合、时间范围）。规则强调**只抽取问题中明确给出的条件，不得脑补；拿不准一律 knowledge**。解析失败重试一次，仍失败回退 `intent=knowledge` 并在回答前加「未能识别为订单查询，按知识库回答。」前缀。
- `execute_order_query(filters, aggregation)`：参数化 SQL 执行器。返回 `{rows, aggregation, truncated}`；未知/非法过滤键直接丢弃并记日志（不报错）。
- `prepare_order_context(route, user_role)`：权限校验（`aftersale`/`admin` 白名单）+ 查询执行；DB 异常按无结果处理，不阻塞回答。

**SQL 模板规则（防注入硬约束）**：

- 过滤子句仅允许：`order_no = ?`（精确）、`customer_name LIKE ?`（自动包 `%...%`）、`product_type = ?`、`payment_method = ?`、`created_at >= ?` / `created_at < ?`（`created_to` 按"次日零点"处理，保证含当天）、`completed_at IS NOT NULL` / `IS NULL`（status=completed/pending）。
- 多条件一律 `AND` 连接，全部 `?` 占位符绑定；**任何情况下不得拼接用户问题或 LLM 原文进 SQL**（测试覆盖注入用例，如 order_no 传入 `"1 OR 1=1"` 不得改变语义）。
- 路由 JSON 中的过滤值在查询前做类型与格式校验：产品型号/支付方式/状态为枚举白名单校验；日期须匹配 `YYYY-MM-DD`，不合规则丢弃该条件并记日志。
- `aggregation=count` → `SELECT COUNT(*)`；`sum`/`avg` 作用于 `total_amount`（`ROUND(..., 2)`），均携带 WHERE。
- 列表查询：按创建时间倒序 `LIMIT 20`（探测查询 21 条以判断 `truncated`）；截断时上下文注明「命中超过 20 条，仅展示最近 20 条」。

**上下文与 Prompt**：

```
【订单数据库查询结果】（共 N 条，仅展示最近 20 条）
| 订单号 | 客户 | 联系方式 | 产品型号 | 数量 | 创建时间 | 完成时间 | 支付方式 | 总金额(元) |
| ... |
```

- 聚合问题仅给出聚合值一行。
- `contact` 在任何面向 LLM 上下文与前端的输出中一律脱敏为前 3 位 + `****` + 后 4 位；数据库内存原文，脱敏发生在结果格式化阶段。
- 上下文含订单数据时切换为订单专用 System Prompt：「订单数据来自订单数据库，必须严格按表格数据回答，不得估算或编造……任何情况下都不得回答'该问题超出我的知识范围'」，且 temperature 强制 0.0，避免数值幻觉。
- 越权订单问题（纯 order 意图）直接返回固定拒绝话术，**不调用 LLM**（杜绝诱导泄漏）；mixed 意图越权时输出拒绝前缀后仍回答知识库部分。无结果时同样不走 LLM，直接返回固定话术。

**为什么不用 MCP / 不做自由 NL2SQL**（详见 `docs/adr/0005`）：

- **MCP** 是"LLM 客户端（宿主）↔ 外部工具服务器"之间的协议。本系统的 Flask 后端自身就是调用 LLM 的宿主，查询订单是进程内函数调用，套一层 MCP 协议零收益、徒增复杂度。
- **自由 NL2SQL**（LLM 直接产出完整 SQL）存在注入风险、执行错误率高、难以评测。白名单模板已覆盖 2.5 节全部问题类型。

### 7.7 LLM 提供方注册表与当前模型切换

**设计目标**：回答生成与意图路由共用一个可由管理员在界面切换的「当前模型」，支持 DeepSeek 与 GLM-5-Base 的对比实验（详见 `docs/adr/0006`）。

**预设提供方注册表**（`backend/llm.py`，硬编码）：

| 字段 | 说明 | deepseek | scnet |
|:---|:---|:---|:---|
| `id` / `name` / `platform` | 标识与展示信息 | deepseek / DeepSeek / DeepSeek 开放平台 | scnet / GLM-5-Base / 国家超算互联网 |
| `base_url` | OpenAI 兼容端点 | `https://api.deepseek.com` | `https://api.scnet.cn/api/llm/v1` |
| `model` | 模型标识（直接写在注册表，不经环境变量） | `deepseek-v4-flash` | `GLM-5-Base` |
| `api_key` | 密钥，**只来自 .env**，不入库不回显 | `DEEPSEEK_API_KEY` | `SCNET_API_KEY` |
| `router_max_tokens` | 意图路由调用 token 预算 | 300 | **2000**（思考型模型预留推理预算） |
| `supports_response_format` | 是否使用 `response_format=json_object` | true | **false**（该端点上会返回乱序文本） |

**当前模型解析顺序**：`app_settings` 表 `llm_provider` 值 → `.env` 的 `LLM_PROVIDER`（默认 deepseek）→ 注册表 deepseek 兜底。DB 值失效（如提供方被移除）时自动回退默认。切换立即对后续请求生效，不影响进行中的回答；重启不丢（DB 持久化）。

**切换与测试**（接口见 6.6，前端 `/settings` 页面）：

- 切换时校验目标提供方密钥已配置，未配置返回 400（`PROVIDER_KEY_MISSING`），前端对未配置项禁用切换按钮。
- 「测试连接」发一次最小真实调用（max_tokens=8）验证端点与密钥可用。
- **失败不降级**：运行时 LLM 调用失败不自动回退另一模型（两模型回答质量不可比，静默换模型会污染评测数据）；流式失败以 SSE `error` 事件显式告知（见 7.5），非流式返回 500。

**思考型模型适配（GLM-5-Base 实测结论）**：

- GLM-5-Base 先输出 `reasoning_content`（推理过程）再输出 `content`（正式答案）；网关不接受 `enable_thinking` / `thinking` 关闭参数。
- 若路由调用 `max_tokens` 过小（如 300），推理即可耗尽全部预算，`content` 为空导致 JSON 解析失败——故 scnet 提供方 `router_max_tokens=2000`。
- 该端点上 `response_format=json_object` 会返回乱序文本（finish=abort），故 `supports_response_format=False`，`invoke_json` 跳过该参数直接用纯 JSON 提示词 + 宽容解析（剥 markdown 代码块、截取首尾大括号）。
- 生成侧防御：`invoke` 返回空内容、或 `stream` 全程无内容且 `finish_reason=length` 时显式报错（提示调大 `LLM_MAX_TOKENS`），不产出空回答。

**新增提供方步骤**（三处配套，前端无需改动；`llm.py` / `config.py` / `.env.example` 内均有注释示例，README 另有完整说明）：

1. `llm.py` 的 `_build_providers()` 注册一项 `ModelProvider`（思考型模型设 `router_max_tokens=2000`，`json_object` 异常端点设 `supports_response_format=False`）；
2. `config.py` 新增该平台的密钥/地址环境变量映射（模型标识直接写在注册表 `model` 字段）；
3. `.env` 填入真实密钥并重启后端，`/settings` 页面自动出现新模型卡片。

### 7.8 增强模式编排设计

**模块与接缝**：新增 `backend/orchestrator.py`，对外暴露与 `rag.answer_question` 同形的入口（返回 SSE 事件生成器），内部负责规划、子答案并行、有界第二轮与合成。它只依赖两个工具与 LLM 端口，不直接接触 `chroma_store` / `embeddings` / `reranker`：

| 工具 | 输入 | 输出 | 权限 |
|:---|:---|:---|:---|
| `knowledge_search(query)` | 检索式 | 按重排分数排序的证据单元候选（最多 3 个，含 doc_id、行区间、原文） | 内部按 `role_permissions` 过滤；调用方**无法**传入角色或领域 |
| `order_query(filters, aggregation)` | 结构化条件 | 行集或聚合值，或「拒绝 / 空结果」状态 | 内部按角色白名单（aftersale/admin）与过滤键白名单校验 |

工具返回值是结构化结果（命中 / 拒绝 / 空 / 截断），**「拒绝」与「无结果」是数据而不是异常**——编排器因此可以统一调度，同时保持「越权不调用 LLM、不泄露订单存在性」的安全约定。规划输出一律视为不可信输入。

**规划输出契约**（一次 LLM 调用产出）：

```json
{
  "needs_decomposition": true,
  "intent": "knowledge",
  "filters": {},
  "aggregation": null,
  "sub_questions": [
    {"id": 1, "query": "2025 动力电池系统 营收", "source": "knowledge"},
    {"id": 2, "query": "DD20260315004", "source": "order",
     "filters": {"order_no": "DD20260315004"}, "aggregation": null},
    {"id": 3, "query": "{1} 单体质量能量密度 25℃ 循环寿命",
     "source": "knowledge", "depends_on": 1}
  ]
}
```

- 子问题写作「实体 + 属性 + 限定条件」，不丢量化条件（年度、温度、口径）。
- `source` 在规划阶段一次定死，执行期不再调用 LLM 判断用哪个工具。
- 链式问题的后续步骤以**待定子问题**表达：`query` 里用 `{前一步 id}` 占位、并标注 `depends_on`；编排器在第二轮用依赖子问题的 `key_entities` 填实占位再检索，因此链式第二跳不依赖"第一轮证据是否恰好够用"。
- `needs_decomposition=false` 时字段与 `order_qa.route_question` 的返回结构一致，直接驱动标准模式单跳路径。
- 规划连续两次解析失败 → 退回标准模式单跳并加显式前缀。
- 规划可产出任意数量的子问题，执行时按上表的「子问题执行上限」截断，被截断的部分记入 trace。

**子答案契约**（每个子问题一次 LLM 调用）：

```json
{"answer": "…", "coverage": "sufficient", "evidence_ids": [1, 3], "key_entities": ["XX-280"]}
```

- `coverage` 取 `sufficient` / `partial` / `missing`，只有后两者触发第二轮。
- `key_entities` 是供下一轮构造检索式的中间实体（如链式问题第一跳查出的电芯型号），编排器直接取用，不解析自然语言。
- 允许多值：候选数有界（≤2）时对每个候选各展开一次，合成时并列呈现；超出则只呈现歧义，不做猜测。
- 调用失败不阻塞其他子问题，该子问题标注失败并在合成结果中显式说明。
- 合成时子答案仅作为线索，事实以证据单元为准；回答中出现的数值必须能在证据中原样找到。

**并发与预算**：

| 参数 | 默认值 | 配置项 |
|:---|:---|:---|
| 子问题执行上限（超出截断并记 trace） | 8 | `AGENT_MAX_SUB_QUESTIONS` |
| 每个子问题的证据单元数 | 3 | `AGENT_EVIDENCE_PER_SUB` |
| 全局证据单元数（去重后） | 10 | `AGENT_EVIDENCE_GLOBAL` |
| 证据单元字符上限 | 3000 | `AGENT_EVIDENCE_MAX_CHARS` |
| 规划调用 token 预算 | 10000 | `AGENT_PLAN_MAX_TOKENS` |
| 子答案调用 token 预算 | 6000 | `AGENT_SUB_ANSWER_MAX_TOKENS` |
| 知识检索工具返回候选数 | 5 | `AGENT_SEARCH_CANDIDATES` |
| 重排后保留的候选数 | 12 | `AGENT_RERANK_TOP_N` |
| 增强模式向量召回调用的 k（重排仍取少量） | 30 | `AGENT_RETRIEVE_K` |
| 子答案调用超时 | 60s | `AGENT_SUB_TIMEOUT` |
| 整轮循环总预算 | 120s | `AGENT_TOTAL_BUDGET` |
| 子答案并发 | 独立线程池，上限 = 子问题执行上限 | — |
| 历史上下文轮数 | 10 轮（20 条） | `CHAT_HISTORY_TURNS` |

并发使用**独立线程池**，不与灌库线程池（`tasks.py`，3 个 worker）共用，避免一次增强问答把灌库任务饿死。用户点击停止时，服务端不强杀已发出的上游调用，编排器收到取消信号后立即停止等待，消息置为 `aborted`，未完成的子问题不落库。

**配额归属**：知识检索工具返回排序候选（最多 3 个），而「每子问题取几个、全局一共几个」由编排器裁决——全局状态属于编排器，工具保持无状态、可被任意次调用（第二轮也是同一把调用）。去重按**证据单元行区间**而非 chunk，否则同一张表的多个分段会被各自展开、重复注入。

**trace 记录**：子问题文本、子答案、证据覆盖、命中的证据单元（doc_id + 行区间）、每步耗时、模型 id、token 用量、是否走单跳短路、被截断的子问题数。随助手消息存入 `messages.trace_json`，随会话删除。

**用户可见性**：子问题与子答案以「分析过程」容器呈现——位于回答上方，流式期间随 `sub_answer` 事件实时填充，回答开始流式输出后默认收起为一行摘要（与引用来源区默认收起一致）；trace 明细不对用户展示。

---

## 8. 数据策略

### 8.1 数据来源
| 领域  | 来源                | 处理方式                        |
| :-- | :---------------- | :-------------------------- |
| 财务  | 巨潮资讯网（宁德时代年报）     | 替换公司名/封面为"星辰科技集团"；财务数值为虚构改写 |
| 规章  | 人社局示范文本、HR公众号模板   | 下载2-3份，合并为统一员工手册            |
| 产品  | 官网/行业数据库（电池参数）    | 下载3-5款产品规格书，替换公司名           |
| 售后  | 电商平台帮助中心、国家"三包"规定 | 整理为标准化的售后政策文档               |
| 订单  | 确定性种子脚本生成          | 50 条编造数据（见 5.2），仅用于问答演示与评测   |

### 8.2 数据预处理
- 所有源文件在上传前，**必须人工/工具转为 Markdown**（剔除图片）。
- 推荐工具：`marker-pdf`（PDF→MD）、`pandoc`（Word→MD）、`pandas.to_markdown()`（Excel→MD）。
- 论文中注明："本系统数据来源于公开渠道并经脱敏处理，仅用于学术研究。"

---

## 9. 项目目录结构

```text
stellar-kb-rag/
├── backend/
│   ├── app.py                      # Flask 应用工厂（CORS、健康检查、蓝图注册）
│   ├── auth.py                     # JWT 认证 + 角色装饰器
│   ├── users_api.py                # 用户管理接口（admin）
│   ├── docs_api.py                 # 知识库管理接口（上传/列表/状态/删除/原文）
│   ├── chat_api.py                 # 问答接口（非流式 + SSE，含 error 事件）
│   ├── conversations_api.py        # 会话接口（列表/新建/回放/删除，限本人会话）
│   ├── orders_api.py               # 订单数据列表接口（过滤/分页/脱敏）
│   ├── settings_api.py             # 模型设置接口（查看/切换/测试，admin）
│   ├── rag.py                      # 标准模式：意图路由集成 + 权限检索 + 重排 + 问答组装
│   ├── orchestrator.py             # 增强模式编排循环（规划/子答案/有界第二轮/合成）
│   ├── agent_tools.py              # 增强模式工具（知识检索 / 订单查询，权限固化在工具内部）
│   ├── order_qa.py                 # 订单意图路由 + 参数化 SQL 执行器
│   ├── llm.py                      # LLM 客户端（预设提供方注册表 + 当前模型解析）
│   ├── embeddings.py               # SiliconFlow Embedding + LangChain 适配
│   ├── chroma_store.py             # Chroma 单一 Collection 存取
│   ├── reranker.py                 # SiliconFlow Reranker 自定义封装类
│   ├── splitter.py                 # Markdown 切片器（文本/表格）
│   ├── tasks.py                    # 后台灌库线程池与流水线
│   ├── models.py                   # SQLite 数据访问层（Schema/种子/CRUD/设置）
│   ├── orders_seed.py              # 订单种子数据（确定性、幂等）
│   ├── config.py                   # 配置（API Key、路径等，读取 .env）
│   ├── errors.py                   # 统一错误响应与错误处理器
│   ├── ingest.py                   # 离线灌库 CLI
│   ├── eval.py                     # 检索评测脚本（Hit Rate / MRR）
│   ├── eval_orders.py              # 订单问答评测脚本（含评测模型标注）
│   ├── e2e_demo.py                 # 本地端到端联调脚本
│   ├── data/                       # 本地持久化目录（gitignored）
│   │   ├── chroma/                 # Chroma 向量数据
│   │   └── app.db                  # SQLite 数据库文件
│   ├── markdown_src/               # 原始 Markdown 源文件（按领域分目录，gitignored）
│   └── tests/                      # 自动化测试（136 个用例）
├── frontend/
│   ├── app/
│   │   ├── layout.tsx              # 全局布局（AntdRegistry）
│   │   ├── page.tsx                # 首页（重定向到 /chat）
│   │   ├── login/page.tsx          # 登录页
│   │   ├── chat/page.tsx           # 问答页（核心）
│   │   ├── knowledge/page.tsx      # 知识库管理（admin）
│   │   ├── users/page.tsx          # 用户管理（admin）
│   │   ├── orders/page.tsx         # 订单数据（aftersale/admin）
│   │   ├── viewer/page.tsx         # 原文档查看与定位（行号高亮）
│   │   └── settings/page.tsx       # 模型设置（admin）
│   ├── components/
│   │   ├── ChatBox.tsx             # 问答组件（流式渲染/中断/失败提示/模式切换）
│   │   ├── AnalysisPanel.tsx       # 增强模式分析过程容器（子问题卡片，默认收起）
│   │   ├── ConversationList.tsx    # 会话列表（新建/切换/删除，5 个上限）
│   │   ├── SourceCard.tsx          # 引用来源卡片（vector/database，按子问题分组）
│   │   └── LayoutWrapper.tsx       # 布局 + 登录/角色路由守卫 + 菜单
│   └── lib/
│       └── api.ts                  # API 调用封装（含 SSE 流式消费与 error/stage 事件）
├── docs/                           # ADR（0001~0009）、Golden Set、评测报告
├── CONTEXT.md                      # 领域词表
├── .env.example                    # 环境变量模板
├── requirements.txt
├── start-dev.cmd                   # Windows 一键启动脚本
└── README.md
```

---

## 10. 测试与评测计划

### 10.1 Golden Set（检索评测）
每个领域构造 **15-20 个典型问答对**，格式如下：
```json
{
  "domain": "finance",
  "question": "星辰科技集团2024年净利润是多少？",
  "answer_must_contain": ["1688亿"],
  "source_file": "2024年报.md"
}
```

> **注**：示例中的 `1688亿` 为虚构改写数值，实际 Golden Set 断言须与预处理后源文档中的数值一致。

### 10.2 检索评测指标
- **Hit Rate**：Top-5 检索结果中是否包含能回答问题的正确 Chunk（目标 ≥ 0.8）。
- **MRR (Mean Reciprocal Rank)**：第一个相关 Chunk 排名的倒数均值（目标 ≥ 0.5）。
- 评测脚本 `backend/eval.py` 走无权限过滤的全领域检索管线，报告写入 `docs/eval_report.md`。

> **指标偏置（ADR 0009）**：Hit Rate 的判定是「Top-5 中是否包含关键词」，这个口径对块大小并不中立——块越大越容易包含关键词，指标会因此虚高。所以它既不能用来给切片粒度做消融，也不能用来比较标准模式与增强模式。评测口径的修订（补充证据覆盖率、答案正确率等）另行处理。

### 10.3 检索评测逻辑（伪代码）
```python
class RAGPipelineForEval:
    """评测专用 RAG 管线（无权限过滤，全领域检索）"""
    def retrieve(self, question: str) -> list:
        """
        完整检索流程：
        1. 在 Chroma 全领域检索 Top-10（不做权限过滤）
        2. 经 SiliconFlow Reranker 重排
        3. 返回 Top-5 Document 列表（每个含 page_content 和 metadata）
        """
        candidates = self.chroma_collection.similarity_search(question, k=10)
        reranked = self.reranker.compress_documents(candidates, question)
        return list(reranked)[:5]


def evaluate(golden_set, rag_pipeline):
    hit, mrr_sum = 0, 0
    for item in golden_set:
        results = rag_pipeline.retrieve(item["question"])
        found = False
        for rank, doc in enumerate(results, 1):
            if any(kw in doc.page_content for kw in item["answer_must_contain"]):
                hit += 1
                mrr_sum += 1 / rank
                found = True
                break
        if not found:
            # 该 query 未命中，MRR 贡献为 0（已隐含在分母中）
            pass
    return {"hit_rate": hit/len(golden_set), "mrr": mrr_sum/len(golden_set)}
```

### 10.4 订单问答 Golden Set 与评测

**Golden Set**：`docs/golden_orders.json`，格式：
```json
{
  "question": "订单 DD20260315004 的总金额是多少？",
  "role": "aftersale",
  "intent": "order",
  "expected": "17000",
  "mode": "exact"
}
```
- 分布：单订单查询、客户查询、时间范围、支付方式、状态过滤、聚合（count/sum/avg）、混合问题各 ≥2 条；另含 `intent: "knowledge"` 干扰项（验证路由不误判）。
- `expected` 数值**必须按真实落库的种子数据填写**（同 10.1 约定）。
- `mode`：`exact`（数值/日期精确匹配，允许千分位与单位差异归一化）或 `contains_all`（列表类，答案须覆盖全部期望订单号）。

**评测脚本**：`backend/eval_orders.py`，走**完整真实管线**（真实路由 LLM + 真实 SQL），输出：

- **答案正确率**（按 mode 判定）——总体与分类型（目标 ≥ 85%）
- **路由准确率**（intent 与 golden 标注比对，含混淆矩阵）（目标 ≥ 90%）

报告写入 `docs/eval_orders_report.md`，含评测日期、**评测模型**（当前模型名，支持跨模型对比实验）、种子订单数与目标值。切换模型后重跑评测即可得到 DeepSeek 与 GLM-5-Base 的对比数据（答辩实验素材）。

### 10.5 自动化测试
- 位置：`backend/tests/`（pytest，V2.0 起新增 splitter 前缀与证据单元、会话接口、编排循环三类用例，随实现同步补齐），覆盖 auth / db / docs / e2e / eval / ingest / order_qa / orders / permission / rag / reranker / splitter / llm providers / settings / chat stream / conversations / orchestrator。
- 路由与 LLM 调用在单元测试中一律 Mock（monkeypatch `llm.invoke` / `llm.stream` / `llm.invoke_json`），不依赖真实 API。
- 订单 SQL 测试覆盖注入用例（`"1 OR 1=1"` 等不得改变语义）、时间边界（含当天）、脱敏输出与分页上限。
- 每个用例使用全新临时数据库与 Chroma 目录（conftest fixture），种子计数可预测。

---

## 11. 部署方案

### 11.1 开发阶段（本地）
- 后端：`flask --app app run --port 5000`
- 前端：`npm run dev`（默认 :3000）
- Windows 一键启动：项目根目录执行 `start-dev.cmd`（自动初始化数据库并分窗启动前后端）
- Chroma：按 `CHROMA_PERSIST_DIR` 配置（默认 `./backend/data/chroma`）
- SQLite：按 `DATABASE_URL` 配置（默认 `./backend/data/app.db`）
- 路径约定：上述默认路径以**项目根目录**为基准；`config.py` 基于自身文件位置解析路径，避免启动目录漂移。

### 11.2 答辩部署（云服务器）
- Chroma 切换：`PersistentClient` → `chromadb.HttpClient(host="服务器IP", port=8000)`（若启用 HTTPS 再加 `ssl=True`）
- Chroma 服务端：`docker run -p 8000:8000 chromadb/chroma`
- 前端构建：`npm run build` → 静态导出或 Node 服务
- 后端：Gunicorn + Nginx 反向代理（SSE 场景建议 Gunicorn 使用线程/异步 worker，并在 Nginx 关闭 `proxy_buffering`，避免流式响应被缓冲）

---

## 附录：关键约束与注意事项

- [ ] LangChain 版本：`requirements.txt` 中固定 `langchain==0.3.*`（同时安装 `langchain-chroma`、`langchain-community`，LangChain 0.3 已将 Chroma 等集成拆分为独立包）
- [ ] Chroma 架构：单一 Collection + metadata 过滤（见 4.1 节）
- [ ] 表格极端情况：表头本身超 1024 字符时，整块存入并在日志中记录警告（见 7.1 节）
- [ ] 订单问答：白名单参数化 SQL，禁止自由 NL2SQL 与 MCP（见 7.6 节，ADR 0005）
- [ ] LLM 切换：预设注册表 + DB 当前模型 + 失败不降级（见 7.7 节，ADR 0006）
- [ ] 账号删除：停用与删除分离，删除前置停用、文档上传者置空（见 6.4 节，ADR 0007）
- [ ] 增强模式：复用「当前模型」、以 `agent_capable` 控制可用性、仅流式、不在背后换模型（见 2.6 / 7.8，ADR 0008）
- [ ] 证据单元：小块检索、大块证据；前缀与证据单元行区间随灌库写入，改动需重灌（见 5.3 / 7.1，ADR 0009）
- [ ] 会话：每用户最多 5 个、消息四态（streaming/completed/failed/aborted）、trace 随会话删除（见 2.7 / 6.9）
- [ ] Chroma 迁移：本地数据目录打包上传服务器，仅改连接方式（注意本地与服务器 Chroma 版本保持一致）
- [ ] 前端开发：严格复用 Ant Design 组件，不花时间在自定义 CSS 上
- [ ] 环境变量：所有 API Key、密钥存储在 `.env`，确保 `.gitignore` 包含之
- [ ] 环境变量完整清单（必须包含）：
  - `SILICONFLOW_API_KEY`：硅基流动 API 密钥
  - `SILICONFLOW_BASE_URL`：默认为 `https://api.siliconflow.cn/v1`
  - `DEEPSEEK_API_KEY`：DeepSeek API 密钥（LLM 预设提供方一）
  - `DEEPSEEK_BASE_URL`：默认为 `https://api.deepseek.com`（注意：不带 `/v1` 后缀，SDK 会自动拼接）
  - `SCNET_API_KEY`：超算互联网 API 密钥（LLM 预设提供方二：GLM-5-Base；不配置则该模型不可切换）
  - `SCNET_BASE_URL`：默认为 `https://api.scnet.cn/api/llm/v1`（OpenAI 兼容端点）
  - `XIAOMI_API_KEY`：小米 API 密钥（LLM 预设提供方：MIMO-V2.5 / MIMO-V2.5-Pro；不配置则该模型不可切换）
  - `XIAOMI_BASE_URL`：默认为 `https://api.xiaomimimo.com/v1`（OpenAI 兼容端点）
  - `LLM_PROVIDER`：当前模型提供方默认值（注册表中的 id，如 `deepseek-v4f` / `scnet-glm5base`），管理员界面切换后以 DB 设置为准
  - `JWT_SECRET_KEY`：JWT 签名密钥（随机生成，不少于 32 字符）
  - `JWT_EXPIRATION_HOURS`：JWT 过期时间，默认 `168`（7 天）
  - `FLASK_CORS_ORIGINS`：允许跨域的前端 Origin，开发环境默认 `http://localhost:3000`
  - `CHROMA_PERSIST_DIR`：Chroma 持久化目录，默认 `./backend/data/chroma`
  - `DATABASE_URL`：SQLite 路径，默认 `./backend/data/app.db`
  - `DATABASE_TIMEOUT`：SQLite 连接超时（秒），默认 `30`（配合 WAL 模式；每个线程使用独立连接）
  - `MAX_CHUNK_SIZE`：文本切片大小，默认 1024
  - `MAX_CHUNK_OVERLAP`：切片重叠长度，默认 50
  - `UPLOAD_MAX_SIZE_MB`：上传文件大小上限，默认 10
  - `LLM_TEMPERATURE`：生成温度，默认 0.3（订单场景固定 0.0）
  - `LLM_MAX_TOKENS`：生成 max_tokens，默认 4096（思考型模型的推理计入该预算，若长回答被截断可调大，如 8192）
  - `AGENT_MAX_SUB_QUESTIONS`：增强模式子问题执行上限，默认 8（超出截断并记 trace）
  - `AGENT_EVIDENCE_PER_SUB`：每个子问题注入的证据单元数，默认 2
  - `AGENT_EVIDENCE_GLOBAL`：全局证据单元上限（去重后），默认 10
  - `AGENT_EVIDENCE_MAX_CHARS`：单个证据单元字符上限，默认 3000
  - `AGENT_PLAN_MAX_TOKENS`：规划调用 token 预算，默认 10000（推理型模型会先花掉大量预算再输出 JSON；实测一次规划用掉约 2900，其中 2767 为推理）
  - `AGENT_SUB_ANSWER_MAX_TOKENS`：子答案调用 token 预算，默认 6000
  - `AGENT_SEARCH_CANDIDATES`：知识检索工具返回的候选数，默认 3
  - `AGENT_RETRIEVE_K`：增强模式的向量召回条数，默认 30（高于标准模式的 10，用于缓解超大文档里具体句子挤不进候选集的问题）
  - `AGENT_SUB_TIMEOUT`：单次子答案调用超时（秒），默认 60
  - `AGENT_TOTAL_BUDGET`：增强模式整轮循环总预算（秒），默认 120
  - `CHAT_HISTORY_TURNS`：会话上下文截取的轮数，默认 10（20 条消息）

> **路径说明**：`CHROMA_PERSIST_DIR`、`DATABASE_URL` 的默认值均以项目根目录为基准；`config.py` 建议基于自身文件位置解析，避免因启动目录不同导致路径漂移。

---

> **文档版本**: V2.0
> **最后更新**: 2026-09-13
> **变更记录**:
> - **V2.0（2026-09-13）**：新增**增强模式**（编排式问答：规划拆解子问题 → 并行子答案 → 有界第二轮 → 逐项合成；复用「当前模型」并以 `agent_capable` 控制可用性、仅支持流式、不在背后换模型，ADR 0008）；新增**会话与消息**（每用户 5 个上限、消息四态、trace 分层存储、服务端统一截断上下文，6.9 / 2.7）；切片策略新增**上下文前缀**与**证据单元行区间**（ADR 0009），原「按文件章节（Heading）分片」待定项关闭；`/api/chat` 改为携带 `conversation_id`、不再接收 `history`；新增 7.8 节编排设计；同步修订 2.3 / 3.3 / 4.1 / 4.2 / 5.1 / 5.3 / 6.0 / 6.2 / 6.6 / 7.1 / 7.5 / 9 节与附录（补齐小米与增强模式环境变量、修正 `LLM_PROVIDER` 取值说明）。
> - **V1.9（2026-08-23）**：账号停用与永久删除分离（PUT /deactivate、PUT /activate 取代 PUT /active，DELETE /:id 恢复删除本义；删除仅限已停用账号，其上传文档保留且上传者置空，ADR 0007）；新增自助修改密码（PUT /api/auth/password，验证当前密码并返回新 token）；同步修订 2.3 / 6.1 / 6.4 节与附录约束。
> - **V1.8（2026-08-22）**：并入《订单结构化问答功能设计文档 V1.0》（订单数据与种子、意图路由与参数化 SQL、订单数据页面、订单问答评测，原文件移除）；新增 7.7 节「LLM 提供方注册表与当前模型切换」（GLM-5-Base 接入与思考型模型适配）；按实际实现修订：前端页面表（补 /orders /viewer /settings）、SQLite Schema（补 orders / app_settings）、健康检查响应（deepseek_key → llm_provider）、SSE 失败事件、目录结构、测试与评测章节、环境变量清单。
> - **V1.7（2026-08-05）**：初版（含原文档查看定位、账号停用启用与重置密码）。
