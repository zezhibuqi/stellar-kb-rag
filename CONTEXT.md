# 企业知识问答系统（stellar-kb-rag）

面向虚构“星辰科技集团”的多领域、多角色企业内部知识问答平台：按角色权限检索五个知识领域，结合检索增强生成回答用户问题并提供引用来源。数据来自公开渠道并脱敏/虚构改写，仅用于学术研究。

## Language

**知识领域（domain）**：
五类业务知识范围之一：`finance`（财务数据）、`regulation`（规章制度）、`product`（产品规格）、`aftersale`（售后政策）、`common`（公共知识）。领域标识用于内部数据与接口，领域名称用于前端展示，二者允许不同。
_Avoid_：部门、业务线、分类

**角色（role）**：
用户在企业知识系统中的权限身份，共五类：`employee`、`finance`、`sales`、`aftersale`、`admin`；角色决定用户可检索的知识领域集合。
_Avoid_：职位、岗位、用户组

**角色权限（permission）**：
角色与知识领域之间的可访问关系；`regulation` 与 `common` 对所有角色开放，其余领域按角色隔离。
_Avoid_：授权、数据范围

**账号停用（deactivated account）**：
被管理员停用的账号不再可用且无法登录，但记录仍保留；管理员可将其恢复启用或永久删除，停用期间用户名仍被占用。
_Avoid_：删除的账号、禁用账号

**账号删除（account deletion）**：
将已停用的账号记录从数据库永久移除，不可恢复，用户名随之释放可重新创建；其上传的文档保留，上传者字段置空。
_Avoid_：真删除、物理删除、清空账号

**文档（document）**：
被灌入知识库的 Markdown 源文件，归属于且仅归属于一个知识领域，是检索与管理的原子对象。
_Avoid_：资料、文件

**切块（chunk）**：
文档按文本或表格拆分后的最小检索单元，标记为 `text` 或 `table`；表格切块保持表头完整，不破坏表格语义。
_Avoid_：片段、段落、分片

**引用来源（source）**：
回答中附带的匹配切块信息，包含文件名、领域、内容预览与定位信息，供用户核对答案依据并可跳转到原文档对应位置。
_Avoid_：引用、参考

**原文档（source document）**：
引用来源对应的完整 Markdown 文档；用户可打开查看全文，并定位到引用片段在原文中的起始行。
_Avoid_：原文、原始文件

**灌库任务（ingestion job）**：
文档上传后由后台执行的“切块 → 向量化 → 入库”处理过程，对外呈现 `pending` / `processing` / `completed` / `failed` 四种状态。
_Avoid_：导入、上传任务

**越权提问（cross-domain question）**：
用户提出的问题指向其角色无权访问的知识领域；系统应通过检索过滤保证回答与引用来源均不泄露越权内容。
_Avoid_：非法提问

**订单（order）**：
售后业务中的一条结构化交易记录，归属于某个客户，包含订单号、产品型号、数量、金额、支付方式与完成时间等字段；订单状态由完成时间是否为空推导。
_Avoid_：单据、交易流水

**订单查询（order query）**：
通过自然语言对订单数据库进行的精确查询，支持单订单、按客户、多条件过滤与聚合统计。
_Avoid_：查单、订单检索

**意图路由（intent routing）**：
问答前由 LLM 判断问题属于订单查询、知识问答还是混合，并抽取结构化过滤条件。
_Avoid_：意图识别、路由分类

**结构化问答（structured QA）**：
基于参数化 SQL 模板对数据库精确查询后再生成自然语言回答的问答方式，与向量检索的知识问答互补。
_Avoid_：数据库问答、NL2SQL

**模型提供方（model provider）**：
预先注册的可切换 LLM 服务选项之一（如 DeepSeek、超算互联网 GLM-5-Base），由服务地址、模型标识与密钥构成；仅管理员可切换，Embedding 与 Reranker 不在其列。
_Avoid_：基座模型、大模型选项

**当前模型（active model）**：
管理员选定的模型提供方；系统的全部 LLM 调用（规划、子答案、合成、意图路由等）均使用它，切换后对后续请求生效、不影响进行中的回答。增强模式的可用性由它是否具备相应能力决定。
_Avoid_：默认模型、生效模型

**标准模式（standard mode）**：
系统默认的问答链路：一次意图路由、一次权限检索与重排、一次生成，不拆分问题。
_Avoid_：普通模式、原始模式

**增强模式（enhanced mode）**：
由规划驱动的问答链路：把问题拆解为可独立检索的子问题，逐个子问题取得子答案并检查证据覆盖，必要时补充检索，最后按子问题逐项合成回答。
_Avoid_：Agent 模式、高级模式、智能模式

**规划（planning）**：
增强模式问答的第一步：判定问题意图、抽取结构化过滤条件并拆解子问题；标准模式下这一动作仅为意图路由，故规划是它的超集。
_Avoid_：任务分解、planning

**子问题（sub-question）**：
增强模式下由规划拆解出的、可独立检索的信息需求，写作"实体 + 属性 + 限定条件"的检索式，不丢失量化条件（如年度、温度）。
_Avoid_：子查询、分解项

**子答案（sub-answer）**：
针对单个子问题的局部答案，附带证据覆盖状态、所依据的证据，以及可供下一轮检索使用的中间实体。
_Avoid_：中间答案、局部结论

**证据覆盖（evidence coverage）**：
子问题的证据充分程度，取值为充分 / 部分 / 缺失；只有部分或缺失才触发补充检索。
_Avoid_：置信度、相关度

**证据单元（evidence unit）**：
检索命中后实际注入上下文的原文范围：表格为其整张表，文本为其所属章节；由切块携带的行区间定位，超长时以命中段为中心截取。
_Avoid_：父块、上下文块

**会话（conversation）**：
某用户与系统的一段连续对话，包含消息序列，每用户最多保留 5 个。
_Avoid_：聊天记录、对话、session

**消息（message）**：
会话中的一轮用户提问或系统回答，记录其所用的模式与状态。
_Avoid_：记录、条目
