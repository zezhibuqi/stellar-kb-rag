
> **元指令**：在实施过程中，若对任何原始需求有不确定、或发现逻辑矛盾、或存在两种以上技术实现路径，必须先以简洁方式提问并列出选项，等待决策。禁止猜测或默认实现。
>
> **定位**：本文档是《项目设计文档 V1.7》的功能扩展篇，面向 vibecoding Agent（Trae）实施。既有系统约定（目录结构、错误格式、SQLite 并发约束、测试风格）全部沿用，不重复描述。

## 1. 背景与目标

### 1.1 背景

现有系统的知识全部来自非结构化 Markdown 文档（向量检索 + Reranker）。售后场景中存在一类典型问题——**订单查询**（"订单 DD20260315004 什么时候完成的？""张伟客户一共有几笔订单？"）。这类问题指向**结构化数据**，向量检索无法保证精确性，聚合类问题（数量/总额）更是无法回答。

### 1.2 目标

为系统新增「订单结构化问答」能力：

- SQLite 新增 `orders` 表，存放编造的订单数据（约 50 条，种子脚本生成）。
- `aftersale` 与 `admin` 角色可在 `/api/chat` 中用自然语言查询订单，获得**精确来自数据库**的回答。
- 其他角色提问订单相关内容时不泄露任何订单数据。
- 问答主流程（SSE、引用来源、多轮历史）行为保持一致。
- 产出订单问答的 Golden Set 与**答案正确率评测**（答案可与数据库真值精确比对，作为 faithfulness 评测的补充）。

### 1.3 非目标

- 不做订单的增删改接口与管理页面（数据由种子脚本一次性编造，仅用于问答演示与评测）。
- 不实现 MCP（Model Context Protocol）。理由见第 3.2 节。
- 不把订单数据灌入 Chroma 向量库（备选基线方案，见 10.6 可选阶段）。

---

## 2. 功能需求

### 2.1 订单数据字段

| 字段 | 说明 | 示例 |
|:---|:---|:---|
| 订单号 `order_no` | 全局唯一，格式 `DD + yyyyMMdd + 3位流水` | `DD20260315004` |
| 客户姓名 `customer_name` | 个人客户或企业联系人姓名 | `张伟` |
| 联系方式 `contact` | 11 位手机号（编造） | `13812345678` |
| 订购物品种类 `product_type` | 产品型号，取值限定为现有产品线的五种型号 | `SC-100` ~ `SC-500` |
| 订购物品数量 `quantity` | 正整数 | `20` |
| 订单创建时间 `created_at` | `YYYY-MM-DD HH:MM:SS` | `2026-03-15 10:24:00` |
| 订单完成时间 `completed_at` | 可为 NULL；NULL 表示订单**未完成** | `2026-03-22 16:00:00` |
| 支付手段 `payment_method` | 枚举：`支付宝` / `微信支付` / `银行转账` / `对公转账` | `银行转账` |
| 总金额 `total_amount` | 单位：元，保留两位小数 | `17000.00` |

> 订单状态（已完成/未完成）不设独立字段，由 `completed_at` 是否为 NULL 推导。

### 2.2 支持的问题类型（面向 aftersale / admin）

| 类型 | 示例问题 | 处理方式 |
|:---|:---|:---|
| 单订单查询 | "订单 DD20260315004 的完成情况？" | 按订单号精确查询 |
| 按客户查询 | "张伟下过哪些订单？" | 客户姓名模糊匹配 |
| 多条件过滤 | "3 月份用银行转账支付的订单有哪些？" | 时间范围 + 支付方式组合过滤 |
| 聚合统计 | "目前未完成的订单有几笔？""6 月订单总金额是多少？" | count / sum / avg 聚合 |
| 混合问题 | "订单 DD20260315004 状态如何？破损商品的退换货流程是什么？" | 订单查询 + 知识检索合并回答 |

### 2.3 权限规则

| 角色 | 订单查询 | 说明 |
|:---|:---|:---|
| `aftersale` | ✅ | 核心目标角色 |
| `admin` | ✅ | 管理员全权限 |
| `employee` / `finance` / `sales` | ❌ | 一律拒绝，不执行任何查询，不泄露订单存在性细节 |

> 权限校验在后端工具执行前完成；前端无需新增入口（订单问答复用 `/chat` 页面）。

### 2.4 越权与无结果的话术

- 越权（非授权角色问订单）：固定回复「订单数据仅售后人员与管理员可查询，如有需要请联系售后部门。」，`sources` 为空。
- 授权角色但查询无结果：如实回复「未查询到符合条件的订单。」，`sources` 附带数据库来源标记（见 6.2）。

---

## 3. 技术方案

### 3.1 总体思路：意图路由 + 参数化 SQL 模板

```
用户问题
   │
   ▼
┌─────────────────────────┐
│ 意图路由（LLM，JSON 输出）│  → {"intent": "order|knowledge|mixed",
└─────────────────────────┘     "filters": {...}, "aggregation": ...}
   │                 │
   │ intent=order    │ intent=knowledge
   │ 或 mixed        │ 或 mixed
   ▼                 ▼
┌──────────────┐   ┌────────────────────────┐
│ 订单查询执行器 │   │ 现有 RAG 管线           │
│ 权限校验       │   │ 权限检索→Reranker→Top-5 │
│ 参数化SQL模板  │   └────────────────────────┘
└──────────────┘             │
   │                         │
   └────────┬────────────────┘
            ▼
   上下文合并 → Prompt → DeepSeek 生成（流式/非流式）
            ▼
   answer + sources（向量来源 + 数据库来源）
```

要点：

1. **路由**：每次问答先做一次轻量 LLM 调用（temperature=0，JSON 输出），判定意图并抽取结构化过滤参数。解析失败时重试一次，仍失败则回退 `intent=knowledge`（保证可用性不劣化）。
2. **执行**：订单查询**只使用参数化 SQL 模板**（`?` 占位符绑定），白名单过滤字段 + 白名单聚合类型；**绝不**让 LLM 自由生成 SQL（防注入、保证可复现、便于评测）。
3. **生成**：查询结果格式化为 Markdown 表格注入上下文，由现有 DeepSeek 调用生成自然语言回答；SSE 流式行为不变。

### 3.2 为什么不用 MCP / 不做自由 NL2SQL

- **MCP** 是"LLM 客户端（宿主）↔ 外部工具服务器"之间的协议。本系统的 Flask 后端自身就是调用 LLM 的宿主，查询订单是进程内函数调用，套一层 MCP 协议零收益、徒增复杂度。**明确禁止**为本功能引入 MCP 依赖。
- **自由 NL2SQL**（LLM 直接产出完整 SQL）存在注入风险、执行错误率高、难以评测。白名单模板已覆盖 2.2 全部问题类型。

> 该决策需落为 ADR 0005（模板见附录 A）。

---

## 4. 数据库设计

### 4.1 Schema（并入 `models.py` 的 SCHEMA 与 `_migrate()`）

```sql
CREATE TABLE IF NOT EXISTS orders (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    order_no TEXT UNIQUE NOT NULL,
    customer_name TEXT NOT NULL,
    contact TEXT,
    product_type TEXT NOT NULL,
    quantity INTEGER NOT NULL,
    created_at TIMESTAMP NOT NULL,
    completed_at TIMESTAMP,
    payment_method TEXT NOT NULL,
    total_amount REAL NOT NULL
);
```

### 4.2 种子数据（`backend/orders_seed.py`，由 `init_db()` 在表为空时调用）

- 约 **50 条**订单，时间跨度 2026-02 至 2026-07；约 1/4 未完成（`completed_at=NULL`）。
- 客户 8~10 人（含 2~3 个多次下单的客户，便于"某客户共有几笔订单"类问题），混合个人与企业客户。
- `product_type` 覆盖 SC-100 ~ SC-500 五种型号；`payment_method` 四种枚举均有分布。
- 金额按型号单价 × 数量计算，单价固定：SC-100=850、SC-200=1200、SC-300=2600、SC-400=4800、SC-500=15000（元）。
- **确定性要求**：数据为硬编码列表或使用固定随机种子，保证任何环境下种子结果一致（评测断言依赖真实数据，见第 9 节）。
- 幂等：表非空时跳过。

### 4.3 脱敏规则

`contact` 在**任何面向 LLM 上下文与前端的输出**中一律脱敏为前 3 位 + `****` + 后 4 位（如 `138****5678`）。数据库内存原文，脱敏发生在查询结果格式化阶段。

---

## 5. 核心逻辑设计

### 5.1 新模块 `backend/order_qa.py`

```python
ORDER_ALLOWED_ROLES = {"aftersale", "admin"}
ORDER_FORBIDDEN_ANSWER = "订单数据仅售后人员与管理员可查询，如有需要请联系售后部门。"
NO_RESULT_ANSWER = "未查询到符合条件的订单。"
LIST_LIMIT = 20          # 列表查询最多返回 20 条
ALLOWED_FILTERS = {"order_no", "customer_name", "product_type",
                   "payment_method", "created_from", "created_to", "status"}
ALLOWED_AGGREGATIONS = {"count", "sum", "avg"}   # 不含 None 以外的键

def route_question(question: str, history: list | None) -> dict:
    """LLM 意图路由：返回 {"intent": "order|knowledge|mixed",
    "filters": dict, "aggregation": None|"count"|"sum"|"avg"}。
    temperature=0，要求纯 JSON 输出；解析失败重试一次，仍失败回退 knowledge。"""

def execute_order_query(filters: dict, aggregation: str | None) -> dict:
    """参数化 SQL 执行。返回 {"rows": [...], "aggregation": {"type":..,"value":..}|None,
    "truncated": bool}。未知过滤键直接丢弃（不报错）。"""

def answer_order_route(question, history, route, user_role, stream):
    """订单/混合路径：权限校验 → 执行查询 → 组装上下文 → 生成回答。"""
```

### 5.2 SQL 模板规则

- 过滤子句仅允许：`order_no = ?`（精确）、`customer_name LIKE ?`（自动包 `%...%`）、`product_type = ?`、`payment_method = ?`、`created_at >= ?` / `created_at < ?`（created_to 按"次日零点"处理，保证含当天）、`completed_at IS NOT NULL` / `completed_at IS NULL`（status=completed/pending）。
- 多条件一律 `AND` 连接，全部 `?` 占位符绑定。
- `aggregation=count` → `SELECT COUNT(*)`；`sum`/`avg` 作用于 `total_amount`（`ROUND(..., 2)`），均携带 WHERE。
- 列表查询：`SELECT * ... ORDER BY created_at DESC LIMIT 20`；命中超过 20 条时 `truncated=True`，回答中需提示"仅展示最近 20 条"。

### 5.3 路由 Prompt（要点）

- 输入：系统说明 + 最近历史（与现有 `format_history` 一致，最多 20 条）+ 当前问题。
- 输出 JSON schema 固定为 5.1 所示结构；附 4~6 条 few-shot 示例（覆盖 order / knowledge / mixed、聚合、时间范围）。
- 规则强调：**只抽取问题中明确给出的条件，不得脑补**；拿不准一律 `knowledge`。

### 5.4 与 `rag.py` 的集成

`answer_question` 签名与返回结构不变，内部流程改为：

```python
def answer_question(question, history=None, user_role="employee", stream=False):
    route = order_qa.route_question(question, history)
    if route["intent"] in ("order", "mixed"):
        order_result = order_qa.prepare_order_context(route, user_role)
        # 越权 → 直接返回固定拒绝话术（stream 时同样以 token+done 事件输出）
    if route["intent"] in ("knowledge", "mixed"):
        candidates = search_with_permission(...)   # 现有逻辑不动
        ...
    context = 合并(订单表格上下文, 知识上下文)
    # Prompt、llm.invoke/stream、sources 组装沿用现有代码
```

- 越权订单问题的拒绝回答**不调用 LLM 生成**（固定话术，杜绝诱导泄漏），但流式接口格式保持一致。
- 无结果时同样不走 LLM，直接返回 `NO_RESULT_ANSWER`。

### 5.5 上下文格式（订单部分）

```
【订单数据库查询结果】（共 N 条，仅展示最近 20 条）
| 订单号 | 客户 | 联系方式 | 产品型号 | 数量 | 创建时间 | 完成时间 | 支付方式 | 总金额(元) |
| ... |
```

聚合问题仅给出聚合值一行。System Prompt 追加一句：「订单数据来自订单数据库，须严格按表格数据回答，不得估算或编造。」

---

## 6. 接口与前端变更

### 6.1 HTTP 接口

- **不新增接口**。`/api/chat` 请求/响应结构不变。
- 无需改动 `users_api` / `docs_api`。

### 6.2 sources 扩展

订单/混合回答的 `sources` 中追加数据库来源项，字段复用现有结构并新增 `source_type`：

```json
{
  "source_type": "database",
  "filename": "订单数据库（SQLite）",
  "domain": "aftersale",
  "content_preview": "命中 3 条订单：DD20260315004、DD20260401002、...",
  "doc_id": null, "chunk_id": null, "chunk_type": null, "start_line": null
}
```

- 向量来源项补 `"source_type": "vector"`（缺省视为 vector，保持向后兼容）。
- `build_sources()` 与各测试断言同步更新（新增字段不得破坏既有断言）。

### 6.3 前端最小改动

- `lib/api.ts`：`ChatSource` 增加 `source_type?: "vector" | "database"`。
- `SourceCard.tsx`：`doc_id` 为 null 时隐藏「查看原文档」按钮；`source_type === "database"` 时标签显示「数据库」。其余页面不动。

---

## 7. 安全与一致性约束

- SQL 全部参数化绑定；**任何情况下不得拼接用户问题或 LLM 原文进 SQL**（测试需覆盖注入用例）。
- 路由 JSON 中的过滤值在入库查询前做类型与格式校验（日期不合规则丢弃该条件并记日志）。
- 联系方式脱敏规则（4.3）在执行器输出处统一执行，测试覆盖。
- SQLite 并发约定沿用：查询使用线程本地连接（`models.get_connection()`）。
- 现有全部测试必须保持全绿；本功能不得修改既有行为（含默认话术、状态机、权限表）。

---

## 8. 测试计划（新增，Mock 外部 API，风格沿用现有 tests/）

| 文件 | 覆盖点 |
|:---|:---|
| `test_orders_db.py` | Schema 字段、种子数据数量/幂等、`completed_at` 推导状态、金额与单价×数量一致 |
| `test_order_qa.py` | 路由器 Mock：order/knowledge/mixed 三分支；SQL 模板正确性（各过滤字段、时间边界含当天、AND 组合）；聚合 count/sum/avg；`truncated` 标记；非法/未知过滤键被丢弃；**注入用例**（order_no 传入 `"1 OR 1=1"` 等不得改变语义）；脱敏输出 |
| `test_rag.py`（扩充） | 越权角色问订单返回固定拒绝话术且不触达 DB；aftersale 正常命中；混合问题同时含数据库与向量来源；无结果话术；SSE 下订单回答事件格式不变 |

路由器的 LLM 调用在测试中一律 Mock（monkeypatch `llm` / 路由函数），与现有测试策略一致。

---

## 9. 评测计划（faithfulness 补充）

### 9.1 Golden Set：`docs/golden_orders.json`（≥20 条）

```json
{
  "question": "订单 DD20260315004 的总金额是多少？",
  "role": "aftersale",
  "intent": "order",
  "expected": "17000",
  "mode": "exact"
}
```

- 分布：单订单查询、客户查询、时间范围、支付方式、状态过滤、聚合（count/sum/avg）、混合问题各 ≥2 条；另含 ≥5 条 `intent: "knowledge"` 的干扰项（验证路由不误判）。
- `expected` 数值**必须在种子数据落库后按真实数据填写**（沿用设计文档 10.1 的约定）。
- `mode`：`exact`（数值/日期精确匹配，允许千分位与单位差异的归一化）或 `contains_all`（列表类，要求答案覆盖全部期望订单号）。

### 9.2 评测脚本：`backend/eval_orders.py`

- 走**完整真实管线**（真实路由 LLM + 真实 SQL），输出：
  - **答案正确率**（按 mode 判定）——总体与分类型；
  - **路由准确率**（intent 字段与 golden 标注比对，含混淆矩阵摘要）。
- 报告写入 `docs/eval_orders_report.md`（含评测日期、种子数据条数、目标值：答案正确率 ≥ 85%，路由准确率 ≥ 90%）。

---

## 10. 开发阶段与验收（Stage O1 → O5，可选 O6）

### Stage O1：订单数据层

- 任务：`orders` 表 Schema + `_migrate()`；`orders_seed.py`（确定性、幂等）；`init_db()` 接入；`test_orders_db.py`。
- 验收：`init_db()` 后表含约 50 条数据；重复执行不重复插入；pytest 全绿。

### Stage O2：路由与查询执行器

- 任务：`order_qa.py`（route_question / execute_order_query / 脱敏）；`llm.py` 增加 JSON 输出调用封装；`test_order_qa.py`（路由 Mock）。
- 验收：模板测试全绿，含注入用例与脱敏用例；路由解析失败回退 knowledge 有测试覆盖。

### Stage O3：问答集成与前端

- 任务：`rag.answer_question` 集成三意图分支；越权/无结果固定话术；`sources` 扩展 `source_type`；`SourceCard` 最小改动；`test_rag.py` 扩充；SSE 回归。
- 验收：aftersale 问订单得到真实 DB 数据回答；employee/finance/sales 得到固定拒绝话术；混合问题两类来源齐全；现有全部 + 新增测试全绿；前端 `doc_id=null` 不再渲染「查看原文档」。

### Stage O4：评测

- 任务：`docs/golden_orders.json`（按真实种子数据填写 expected）；`eval_orders.py`；生成 `docs/eval_orders_report.md`。
- 验收：一键运行产出报告；答案正确率 ≥ 85%、路由准确率 ≥ 90%，未达标时定位迭代（路由 few-shot / 模板覆盖）。

### Stage O5：文档收尾

- 任务：新增 ADR 0005（附录 A 模板）；`CONTEXT.md` 新增词条：订单（order）、订单查询（order query）、意图路由（intent routing）、结构化问答（structured QA）；README 功能特性与「测试与评测」一节补充订单问答说明。
- 验收：文档与实现一致；CONTEXT 词条遵循现有格式（含 Avoid）。

### Stage O6（可选）：向量化基线对比实验

- 任务：将种子订单文本化灌入 Chroma（aftersale 领域），在**同一组** golden_orders 问题上运行纯向量管线，对比答案正确率。
- 产出：对比表写入 `docs/eval_orders_report.md` 附录，作为论文"结构化问答优于纯向量检索"的实验依据。
- 触发条件：O1~O5 验收通过后由用户决定是否执行。

---

## 11. 风险与约束

- **路由误分类**：订单问题被误判为 knowledge 时，答案质量退化但不泄露数据（检索层仍有权限过滤）；通过 few-shot 与评测迭代压低误判率。
- **额外时延**：每次问答多一次小模型路由调用（temperature=0，max_tokens≈300）；可接受，不做缓存。
- **种子与评测耦合**：expected 必须来自真实落库数据；种子脚本确定性是硬约束（4.2）。
- **依赖零新增**：不引入 MCP SDK、不引入 NL2SQL 库；openai SDK 的 `response_format={"type": "json_object"}` 若不可用，回退"纯 JSON 提示词 + 解析重试"。
- **历史兼容**：`sources` 新增字段必须向后兼容（旧消费方不感知）。

---

## 12. 订单数据页面（新增）

为售后与管理员提供订单数据库的可视化页面，用于查看与筛选订单，不提供增删改。

### 12.1 接口

`GET /api/orders`（仅 `aftersale` / `admin`，其余角色返回 403）

| 参数 | 说明 |
|:---|:---|
| `order_no` / `customer_name` | 精确 / 模糊过滤 |
| `product_type` / `payment_method` / `status` | 枚举过滤（`status=completed/pending`） |
| `created_from` / `created_to` | 日期范围（含当天） |
| `page` / `page_size` | 分页（默认 1 / 20，上限 100） |

响应：`{items, total, page, page_size}`；`contact` 脱敏（前 3 位 + `****` + 后 4 位），`status` 由 `completed_at` 推导，默认按创建时间倒序。

### 12.2 页面

- 路由 `/orders`，菜单“订单数据”，仅 `aftersale` / `admin` 可见；无权限访问由前端守卫跳回 `/chat`。
- 筛选区：订单号、客户、产品型号、支付方式、状态、创建时间范围 + 查询/重置。
- 分页表格列：订单号、客户、联系方式（脱敏）、产品型号、数量、创建时间、完成时间、支付方式、总金额、状态。

---

## 附录 A：ADR 0005 模板（Stage O5 落盘至 `docs/adr/`）

```markdown
# 订单结构化问答采用意图路由 + 参数化 SQL 模板

订单类问题通过 LLM 意图路由抽取结构化过滤条件，由后端以白名单参数化 SQL 模板查询 SQLite `orders` 表，结果注入上下文后生成回答；不做自由 NL2SQL，也不引入 MCP。

**Considered Options**：自由 NL2SQL（灵活但注入风险高、执行错误率高、难评测）；订单文本化入向量库（改动最小但精确查询与聚合不可靠，仅作对比基线）；MCP 工具服务器（本系统后端即 LLM 宿主，进程内调用无需协议层）。

**Consequences**：查询能力受白名单模板约束（新增问题类型需扩展模板）；每次问答增加一次路由调用；订单权限采用角色白名单（aftersale/admin）而非 role_permissions 领域表，因订单不是可灌库的文档领域。
```

---

> **文档版本**: V1.0
> **最后更新**: 2026-08-20
> **状态**: 待实施
> **变更记录**: 初版，基于 2026-08-19 需求评估结论编制
