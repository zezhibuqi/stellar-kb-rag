# 项目开发文档 V2.0

> **基于**：《项目设计文档 V2.0》（下称「设计文档」，与本文档同目录）
> **用途**：供 vibecoding 的 Agent 按阶段实施本项目，明确开发顺序、每阶段任务与可测试的验收标准。
> **元指令（沿用设计文档）**：实施过程中，若对任何原始需求有不确定、或发现逻辑矛盾、或存在两种以上技术实现路径，必须先以简洁方式提问并列出选项，等待决策。禁止猜测或默认实现。

---

## 0. 文档使用规则

### 0.1 阶段模型

- V1.0 的 **Stage 0 ~ Stage 8 已全部完成**并通过本地验收（汇总见附录 C，详细任务与验收记录见 V1.0 版本）。
- 本文档的**新增阶段为 Stage 9 ~ Stage 12**，对应设计文档 V2.0 引入的三块工作：切片器与证据单元、会话与消息、增强模式。
- 每个阶段都有明确的**验收标准**，验收通过后才能进入下一阶段。
- 阶段验收 = 自动化测试/脚本检查 + 人工抽查（用户确认）。Agent 必须在每个阶段结束时展示验收结果（测试输出、接口返回、页面截图等）。
- 若验收失败，Agent 修复后重新验收；同一阶段连续两次验收失败，应停下来向用户说明原因与可选方案。
- 实施顺序：`Stage 9 → 10 → 11 → 12`。

### 0.2 全局约定

- 目录结构以设计文档第 9 节为准；数据路径以**项目根目录**为基准（若在 `backend/` 内启动，路径相应调整，见设计文档 11.1）。
- 所有 API Key、密钥存 `.env`，确保 `.gitignore` 包含 `.env`，不提交 Git。
- 依赖锁定：`langchain==0.3.*` + `langchain-chroma` + `langchain-community`；`chromadb>=0.5`；`Flask>=3.0`；Next.js 14.x；TypeScript ≥5.0；Ant Design 5.x。
- SQLite：启用 WAL，`timeout=30`，**每个线程使用独立连接**（sqlite3 连接默认不可跨线程共享）。
- **V2.0 新增约定**：
  - 每个阶段在**独立分支**上开发，提交信息使用中文；**合并回 `main` 与推送远端前，必须由用户进行人工验收**。
  - Stage 9 的改动会令已有向量失效：源数据重做与重灌必须排在其后，且**只灌一次**。
  - 增强模式的 LLM 调用与工具在单元测试中一律 Mock，不依赖真实 API；并发使用独立线程池，不与灌库线程池（`tasks.py`）共用。
  - 术语以 `CONTEXT.md` 为准（标准模式 / 增强模式 / 规划 / 子问题 / 子答案 / 证据覆盖 / 证据单元 / 会话 / 消息）；架构决策见 `docs/adr/0008`、`0009`。

### 0.3 阶段依赖关系

| 阶段 | 名称 | 状态 | 依赖 | 主要产出 |
|:--|:--|:--|:--|:--|
| 0~8 | 骨架 / 数据库 / 认证 / 知识库 / 灌库 / 检索重排 / 问答与 SSE / 测评 / 本地联调 | ✅ 已完成 | — | 见附录 C |
| 9 | 切片器与证据单元 | 待实施 | — | 上下文前缀 + 证据单元行区间 + 展开函数 + 测试 |
| 10 | 会话与消息 | 待实施 | — | 两张表、会话接口、`/api/chat` 状态化、前端会话列 |
| 11 | 增强模式后端 | 待实施 | 9、10 | 工具、编排循环、能力标志、`stage` 事件、配置项 |
| 12 | 增强模式前端与端到端联调 | 待实施 | 11 | 模式切换、分析过程容器、来源分组、`/settings` 标签 |

> **硬依赖只有一条**：Stage 11 依赖 Stage 9 产出的证据单元 metadata。Stage 10 与 Stage 11 互相独立，但 Stage 10 会改动 `/api/chat` 契约，建议先做，使前端只改一次。

### 0.4 待决事项（实施到对应阶段前需用户确认）

1. **源数据重做**：由用户负责修改 Markdown 源文件；**重灌必须发生在 Stage 9 完成之后**（数据文件可随时改，灌库时机不能提前）。
2. **评测口径修订**：由用户负责。现有 Hit Rate 判定对块大小不中立（块越大越容易包含关键词），既不能用于切片粒度消融，也不能用于标准/增强模式对比（见 ADR 0009、设计文档 10.2）。
3. **增强模式的可用模型**：目前仅 DeepSeek 系提供方带 `agent_capable`；是否扩展到其他快速模型，由管理员在 `llm.py` 注册表中调整。
4. **部署**：线上部署仍暂缓（约一年后），Stage 12 只做本地端到端联调。

---

## Stage 9：切片器与证据单元

### 目标

让表格块带上章节归属与表名/单位口径，并把「检索单元」与「证据单元」分离——切块继续只承担检索职责（尺寸不变），实际注入上下文的是证据单元。

### 任务

- [ ] `backend/splitter.py`
  - 扫描时维护标题栈（`^#{1,6}\s+`），为每个块记录「最近一级标题」与「表格上方最近的非空非表格行」（向上最多回看 3 行、跳过空行；该行本身是标题时不重复拼接）。
  - 表格块：入库文本 = 前缀 + 原表格；**超长表格的每个分段都要带前缀**。
  - 文本块：仅对同一章节内被切分出的**第 2 段及以后**补最近标题（第 1 段本就含标题）。
  - 前缀不计入 `chunk_size` 预算，但参与向量化与重排；`start_line` 仍指向原内容首行。
  - 输出新增 `parent_type`（`table` / `section`）、`parent_start_line`、`parent_end_line`：
    - 表格：整张表的首尾行；
    - 文本：所属章节范围（从最近标题到下一个同级或更高级标题之前；文档首块之前以文档首行为起点）；
    - 无标题文档：退化为「命中块 + 前后各一个相邻块」。
- [ ] `backend/chroma_store.py`：把上述三个字段写入每条向量的 metadata。
- [ ] **证据单元展开函数**：按 `(doc_id, parent_start_line, parent_end_line)` 从 `documents.source_content` 切片；超过 3000 字符时以命中段为中心向两侧截取，截取落在完整行边界上（不切断表格行）；按 `(doc_id, parent_start_line, parent_end_line)` 去重。
- [ ] `backend/tests/test_splitter.py`：补齐边界用例（见验收标准）。

### 验收标准

- [ ] `test_splitter.py` 覆盖：空行上方的说明行被取到、上方直接是标题时不重复拼接、文档开头即表格时前缀为空、超长表格多段各自带前缀、只有标题没有说明行、文本块第 1 段不加标题而第 2 段以后加、无标题文档的退化证据单元。
- [ ] 灌库后 Chroma metadata 含 `parent_type` / `parent_start_line` / `parent_end_line`，且与原文行区间一致。
- [ ] 证据单元展开：命中表格的任一分段都能取回整张表；命中多段时去重后只注入一次。
- [ ] `pytest backend/tests -q` 全绿。

### 交付物

- `splitter.py`、`chroma_store.py`、证据单元展开函数、`test_splitter.py`。

### 注意事项

- 本阶段结束前**不要重灌**：已有向量会全部失效，重灌需要与源数据重做合并成一次。
- 前缀参与向量化是刻意的——它正是为了让行级取数类问题能召回正确的表。

---

## Stage 10：会话与消息

### 目标

把问答从无状态改为会话组织：会话与消息落库、上下文由服务端统一截断、消息状态可回放；前端提供会话列表。

### 任务

- [ ] `backend/models.py`
  - 建表 `conversations`（`id / user_id / title / created_at / updated_at`）与 `messages`（`id / conversation_id / role / content / mode / status / sources_json / trace_json / created_at`，`ON DELETE CASCADE`）。
  - CRUD：创建会话（校验每用户 5 个上限）、列出本人会话（按 `updated_at` 倒序）、读取会话消息、删除会话、追加消息、更新消息状态与内容。
- [ ] 新增 `backend/conversations_api.py`：`GET /api/conversations`、`POST /api/conversations`、`GET /api/conversations/:id/messages`、`DELETE /api/conversations/:id`。
  - 非本人会话一律返回 **404**（而非 403，避免通过状态码暴露他人会话是否存在）。
  - 新建时标题为空占位，首条用户消息落库后由服务端用其前 20 字（去除换行）更新，不调用 LLM。
  - 已达上限时 `POST` 返回 400（`CONVERSATION_LIMIT`）。
- [ ] `backend/chat_api.py`
  - `/api/chat` 强制携带 `conversation_id`；**不再接收 `history`**，上下文由服务端从消息表读取并截取最近 10 轮（`CHAT_HISTORY_TURNS`）。
  - 用户消息在请求校验通过后落库；助手消息在流开始时落库为 `streaming`，其 id 通过首个 SSE 事件返回前端；结束时更新为 `completed` / `failed`，用户停止由前端上报置 `aborted`（保留已收到的部分内容）。
- [ ] 前端：新增 `components/ConversationList.tsx`；`/chat` 布局改为「全局导航 + 会话列表 + 聊天区」，窄屏（约 <1200px）会话列收起为抽屉；`lib/api.ts` 增加会话接口封装；`ChatBox.tsx` 接入会话（切换回放、新建、删除二次确认、达上限时禁用新建并提示先删除一个）。
- [ ] 停止按钮：本地立即标记「已停止」，并上报服务端置 `aborted`。
- [ ] `backend/tests/test_conversations.py`：会话 CRUD、归属校验、上限、消息四态流转。

### 验收标准

- [ ] 会话列表按更新时间倒序；切换会话能回放消息与来源。
- [ ] 第 6 个会话创建返回 400（`CONVERSATION_LIMIT`），前端新建入口禁用并提示先删除一个。
- [ ] 他人会话的读取与删除均返回 404。
- [ ] 流式问答进行中刷新页面，会话里该条消息为 `streaming`；结束后为 `completed`。
- [ ] 失败的消息状态为 `failed`、用户停止的为 `aborted`，且都保留已收到的部分内容。
- [ ] 上下文截断只由服务端决定，前端不再发送 `history`。
- [ ] 标准模式的问答行为与 V1.9 一致（仅上下文来源改变）。
- [ ] `pytest backend/tests -q` 全绿。

### 交付物

- `models.py` 增补、`conversations_api.py`、`chat_api.py` 改造、`ConversationList.tsx`、`ChatBox.tsx` 与 `lib/api.ts` 改造、会话相关测试。

### 注意事项

- 本阶段**不保留无会话的旧调用方式**：前端是唯一消费方，同步替换、不留兼容负担（沿用 ADR 0007 的先例）。
- `/api/chat` 契约在本阶段变化一次；Stage 11 只在其上增加 `mode`，不再次改动结构。

---

## Stage 11：增强模式后端

### 目标

实现编排式问答：规划拆解子问题 → 并行子答案 → 有界第二轮 → 逐项合成，并把可用性绑定到当前模型的能力标志。

### 任务

- [ ] `backend/llm.py`：`ModelProvider` 新增 `agent_capable`（DeepSeek 系提供方开启）；`backend/settings_api.py` 的 providers 元素增加该字段。
- [ ] 新增 `backend/agent_tools.py`
  - `knowledge_search(query)`：内部完成权限过滤（`role_permissions`）+ 重排 + 证据单元展开，返回按分数排序的候选（最多 `AGENT_SEARCH_CANDIDATES` 个）；调用方**无法**传入角色或领域。
  - `order_query(filters, aggregation)`：复用现有白名单参数化 SQL；**「拒绝」与「无结果」作为数据返回而非异常**。
  - 返回值统一为结构化结果（命中 / 拒绝 / 空 / 截断）。
- [ ] 新增 `backend/orchestrator.py`
  - 规划（一次 LLM 调用）：`needs_decomposition / intent / filters / aggregation / sub_questions[{id, query, source, filters?, aggregation?}]`；解析失败重试一次，仍失败退回标准模式单跳并加显式前缀。
  - 单跳短路：`needs_decomposition=false` 时走单次生成，**只写 trace，界面不提示**。
  - 子答案：每个子问题一次调用、独立线程池并行；产出 `answer / coverage / evidence_ids / key_entities`。
  - 有界第二轮：仅对 `partial` / `missing` 触发，查询式由 `key_entities` 驱动；多候选（≤2 个）逐个展开并并列呈现。
  - 合成：按子问题逐项落位，缺项显式说明；数值必须能在证据中原样找到。
  - 配额与预算：每子问题 2 个证据单元、全局 10 个、单元上限 3000 字符、子问题执行上限 8（超出截断并记 trace）、子答案超时 60s、整轮预算 120s。
  - 失败语义按设计文档 2.6 实现：**故障显式提示，正常判定静默**。
  - trace：子问题、子答案、证据覆盖、证据行区间、每步耗时、模型 id、token 用量、是否短路、截断数。
- [ ] `backend/chat_api.py`
  - `mode` 取 `standard` / `enhanced`；校验 `MODE_UNAVAILABLE`（当前模型无 `agent_capable`）与 `MODE_REQUIRES_STREAM`（enhanced 且非流式）。
  - SSE 新增 `stage` 事件：`planning`（首个事件，带 `message_id`）、`planned`、`sub_answer`、`synthesizing`。
  - `sources` 每项增加 `sub_question_id`（标准模式为 `null`）。
- [ ] `backend/config.py` 与 `.env.example`：`AGENT_MAX_SUB_QUESTIONS` / `AGENT_EVIDENCE_PER_SUB` / `AGENT_EVIDENCE_GLOBAL` / `AGENT_EVIDENCE_MAX_CHARS` / `AGENT_SEARCH_CANDIDATES` / `AGENT_SUB_TIMEOUT` / `AGENT_TOTAL_BUDGET` / `CHAT_HISTORY_TURNS`。
- [ ] `backend/tests/test_orchestrator.py`：规划契约解析与回退、配额与去重、失败分支、SSE 事件序列（LLM 与工具均 Mock）。

### 验收标准

- [ ] **标准模式无回归**：现有测试全绿，行为与 V1.9 一致。
- [ ] 并列型问题（如「2025 年动力电池系统的营收 / 占比 / 毛利率 / 销量？」）产出 4 个子问题，各自带子答案与证据覆盖状态。
- [ ] 链式型问题（如「为 SC-500 工商业储能一体柜供电的那款电芯，它的单体质量能量密度和 25℃ 循环寿命分别是多少？」）第二跳由第一跳的 `key_entities` 构造查询并命中规格表。
- [ ] 当前模型无 `agent_capable` 时 `mode=enhanced` 返回 400（`MODE_UNAVAILABLE`）；非流式返回 400（`MODE_REQUIRES_STREAM`）。
- [ ] 越权：全部子问题被订单工具拒绝时不调用 LLM，直接返回固定话术；混合问题中知识部分照常回答。
- [ ] 单跳问题不触发拆解（trace 有标记，界面无提示）。
- [ ] 单个子答案调用失败不阻塞其他子问题；全部失败时退回标准模式重试一次。
- [ ] `pytest backend/tests -q` 全绿。

### 交付物

- `agent_tools.py`、`orchestrator.py`、`llm.py` 与 `settings_api.py` 增补、`chat_api.py` 改造、`config.py` 与 `.env.example` 增补、编排测试。

### 注意事项

- 规划输出**一律视为不可信输入**：权限与过滤条件只能由工具内部裁决。
- 增强模式仅支持流式；不要为「非流式增强」增加分支。

---

## Stage 12：增强模式前端与端到端联调

### 目标

把增强模式的过程与结果呈现出来，并完成两个目标问题的端到端验证。

### 任务

- [ ] 模式切换：输入框左侧 `Segmented`（标准 / 增强），选择记忆在 localStorage；当前模型不支持时禁用并说明原因。
- [ ] 新增 `components/AnalysisPanel.tsx`：位于回答上方；`planned` 后显示子问题列表，`sub_answer` 按完成即逐个浮现（子答案 + 证据覆盖状态）；回答开始流式输出后默认收起为一行摘要（「本次拆解为 N 个子问题」），点击可展开。
- [ ] `components/SourceCard.tsx`：按 `sub_question_id` 分组渲染；标准模式（无该字段）保持现有平铺展示；订单来源挂到对应子问题。
- [ ] `app/settings/page.tsx`：提供方卡片增加只读标签「支持增强模式 / 仅标准模式」。
- [ ] 停止按钮：增强模式下在规划与子答案阶段同样可停止，本地立即标记「已停止」。
- [ ] 端到端联调：按设计文档 2.6 的并列型与链式型两个示例问题跑通，并覆盖越权与失败分支。

### 验收标准

- [ ] 标准模式下界面与 V1.9 一致（无分析过程容器、来源平铺）。
- [ ] 增强模式：提问后先出现「规划中」，随后子问题卡片逐个出现，再进入回答流式输出，结束后过程容器收起。
- [ ] 切换会话、刷新页面后，增强模式的过程与分组来源可从消息记录回放。
- [ ] 当前模型不支持增强模式时，切换入口禁用并给出原因。
- [ ] `npm run build` 通过；`pytest backend/tests -q` 全绿。
- [ ] 联调结果写入 `docs/`（沿用 `docs/stage8-local-e2e.md` 的报告格式）。

### 交付物

- `AnalysisPanel.tsx`、`ChatBox.tsx` 与 `SourceCard.tsx` 改造、`/settings` 页面增补、联调报告。

---

## 13. 全局验收清单（Definition of Done）

| 阶段 | 核心验收 | 通过标志 |
|:--|:--|:--|
| 9 | 前缀与证据单元 | 切片器边界用例全绿；metadata 含行区间；命中任一分段可取回整表并去重 |
| 10 | 会话与消息 | 会话 CRUD 与归属校验正确；消息四态完整；上下文由服务端截断 |
| 11 | 增强模式后端 | 并列型/链式型示例跑通；能力标志与两处 400 生效；越权不调用 LLM；失败分支可控 |
| 12 | 前端与联调 | 过程容器与来源分组可回放；标准模式无回归；`npm run build` 与 pytest 全绿 |

---

## 14. 风险与注意事项

- **SQLite 并发**：WAL 只解决读写锁，不解决连接跨线程问题；后台任务与编排器线程必须各自建连接。
- **SSE**：`EventSource` 仅支持 GET，`/api/chat` 是 POST，前端必须用 `fetch + ReadableStream`；生产环境 Nginx 需关闭 `proxy_buffering`。
- **延迟**：增强模式的调用次数随子问题数增长；思考型模型单次调用已达约 15s，因此增强模式的可用性绑定快速模型，且过程事件必须实时推送，否则界面会长时间静止。
- **上下文膨胀**：1M 窗口不是约束，真正的约束是重排判别力与检索侧成本；证据单元按配额裁决、按行区间去重，不要因为窗口大就无节制注入。
- **重灌时机**：Stage 9 与源数据重做必须合并为同一次灌库，否则要灌两遍。
- **提示注入面变宽**：增强模式会把文档内容读进规划步，工具结果必须只当数据用，规划输出不得影响权限与过滤白名单。
- **评测口径**：现有 Hit Rate 对块大小不中立，且只测召回不测答案质量；在口径修订前，不要用它给切片粒度或模式对比下结论。
- **术语一致**：新增概念一律使用 `CONTEXT.md` 中的词条，不要引入同义词。

---

## 附录 A：验收命令速查

```bash
# 测试（Mock 外部 API）
.\.venv\Scripts\python.exe -m pytest backend/tests -q

# 健康检查
curl http://localhost:5000/api/health

# 登录与当前用户
curl -X POST http://localhost:5000/api/auth/login -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"123456"}'
curl http://localhost:5000/api/auth/me -H "Authorization: Bearer <token>"

# 会话
curl http://localhost:5000/api/conversations -H "Authorization: Bearer <token>"
curl -X POST http://localhost:5000/api/conversations -H "Authorization: Bearer <token>"
curl http://localhost:5000/api/conversations/<id>/messages -H "Authorization: Bearer <token>"

# 问答（标准模式）
curl -X POST http://localhost:5000/api/chat -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"conversation_id":1,"question":"公司的核心价值观是什么？","stream":true}'

# 问答（增强模式，观察 stage 事件）
curl -N -X POST http://localhost:5000/api/chat -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"conversation_id":1,"mode":"enhanced","stream":true,"question":"2025 年动力电池系统的营收 / 占比 / 毛利率 / 销量？"}'

# 灌库（必须在 Stage 9 之后执行）
.\.venv\Scripts\python.exe backend\ingest.py --domain finance --path backend\markdown_src\finance
```

## 附录 B：阶段与设计文档章节对照

| 阶段 | 主要依据的设计文档章节 |
|:--|:--|
| 9 | 7.1（规则 6~8）、5.3、ADR 0009 |
| 10 | 2.7、5.1、6.2、6.9 |
| 11 | 2.6、6.2、7.8、ADR 0005、ADR 0008 |
| 12 | 2.3、2.6、6.9、7.5、ADR 0008 |

## 附录 C：已完成阶段（0~8）汇总

| 阶段 | 名称 | 验收结论 |
|:--|:--|:--|
| 0 | 项目骨架与基础设施 | 前后端可启动，`/api/health` 返回组件状态 |
| 1 | 数据库层 | Schema + 种子（5 领域 / 16 条权限）；WAL 与多线程独立连接测试通过 |
| 2 | 认证与用户管理 | 登录/me、管理员创建用户、角色与停用启用、重置密码与自助改密；非 admin 接口 403 |
| 3 | 知识库管理（异步任务框架） | 上传返回 202，状态机 pending→processing→completed/failed，并发上限 3 |
| 4 | 切片与灌库流水线 | 切片器（文本/表格）与 Embedding、Chroma 单集合入库；删除同步清理向量 |
| 5 | 检索与权限过滤 + Reranker | 各角色检索结果不超出权限领域；重排降序且保留 metadata |
| 6 | 问答与 SSE | 非流式与流式均正确；越权不泄漏；sources 字段完整 |
| 7 | 测试与评测 | Golden Set 75 条；Hit Rate 88% / MRR 0.83（product 领域待数据重做后复测）；订单问答 21 条正确率 95.24% |
| 8 | 本地联调（部署暂缓） | 10 步旅程全部通过，报告见 `docs/stage8-local-e2e.md`；当前 `pytest` 149 用例全绿 |

> 阶段 0~8 的详细任务清单与验收记录见本项目《项目开发文档 V1.0》（Git 历史中保留）。

---

> **文档版本**: V2.0
> **最后更新**: 2026-09-13
> **状态**: 待执行（Stage 9~12）
> **变更记录**:
> - **V2.0（2026-09-13）**：基于《项目设计文档 V2.0》编制。Stage 0~8 压缩为附录 C 汇总；新增 Stage 9（切片器与证据单元）、Stage 10（会话与消息）、Stage 11（增强模式后端）、Stage 12（增强模式前端与联调）四个阶段的任务与验收标准；补充 V2.0 全局约定（分支与人工验收、重灌时机、线程池隔离、术语以 `CONTEXT.md` 为准）；更新全局验收清单、风险清单与验收命令。
> - **V1.0（2026-08-06）**：初版，基于《项目设计文档 V1.7》编制，含 Stage 0~8。
